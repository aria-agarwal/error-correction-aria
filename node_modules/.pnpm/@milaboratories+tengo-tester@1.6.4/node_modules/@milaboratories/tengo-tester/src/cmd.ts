#!/usr/bin/env node

import { spawn } from 'node:child_process';
import { TengoTesterBinaryPath } from './lib';

const child = spawn(TengoTesterBinaryPath, process.argv.slice(2), {
  stdio: 'inherit'
});

child.on('exit', (code) => {
  process.exit(code ?? 0);
});
