#!/usr/bin/env node
"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// src/postinstall.ts
var import_node_path2 = __toESM(require("path"), 1);
var import_node_fs = __toESM(require("fs"), 1);

// src/lib.ts
var import_node_path = __toESM(require("path"), 1);
var import_node_os = __toESM(require("os"), 1);
var DIST_DIR = import_node_path.default.resolve(__dirname);
var BASE_BIN_NAME = "tgo-test";
var OS_NAME = import_node_os.default.platform();
var ARCH_NAME = import_node_os.default.arch();
var BIN_NAME_WITH_OS_ARCH = `${BASE_BIN_NAME}-${OS_NAME}-${ARCH_NAME}`;
var TengoTesterBinaryPath = OS_NAME === "win32" ? import_node_path.default.join(DIST_DIR, `${BIN_NAME_WITH_OS_ARCH}.exe`) : import_node_path.default.join(DIST_DIR, BIN_NAME_WITH_OS_ARCH);

// src/postinstall.ts
function cleanDist() {
  const binaries = import_node_fs.default.readdirSync(DIST_DIR);
  console.log(
    `Cleaning up ${DIST_DIR} directory from unused files and renaming the correct one...`
  );
  let ok = false;
  for (const file of binaries.values()) {
    if (!file.startsWith(BASE_BIN_NAME)) continue;
    if (!file.startsWith(BIN_NAME_WITH_OS_ARCH))
      import_node_fs.default.rmSync(import_node_path2.default.join(DIST_DIR, file));
    else ok = true;
  }
  if (!ok) throw new Error("Can't find executable file.");
}
try {
  if (process.env.INIT_CWD === process.cwd()) process.exit();
  cleanDist();
} catch (e) {
  console.log("tengo-tester postinstall failed: " + e);
}
//# sourceMappingURL=postinstall.cjs.map