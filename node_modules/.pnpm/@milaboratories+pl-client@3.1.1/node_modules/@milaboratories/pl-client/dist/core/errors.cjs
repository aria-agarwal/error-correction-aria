require("../_virtual/_rolldown/runtime.cjs");
const require_code = require("../proto-grpc/google/rpc/code.cjs");
let _milaboratories_ts_helpers = require("@milaboratories/ts-helpers");
//#region src/core/errors.ts
function isConnectionProblem(err, nested = false) {
	if (err === void 0 || err === null) return false;
	if (err instanceof DisconnectedError) return true;
	if (err.name == "RpcError" && err.code == "UNAVAILABLE") return true;
	if (err.name == "RESTError" && err.status.code == require_code.Code.UNAVAILABLE) return true;
	if (err.cause !== void 0 && !nested) return isConnectionProblem(err.cause, true);
	return false;
}
function isUnauthenticated(err, nested = false) {
	if (err === void 0 || err === null) return false;
	if (err instanceof UnauthenticatedError) return true;
	if (err.name == "RpcError" && err.code == "UNAUTHENTICATED") return true;
	if (err.name == "RESTError" && err.status.code == require_code.Code.UNAUTHENTICATED) return true;
	if (err.cause !== void 0 && !nested) return isUnauthenticated(err.cause, true);
	return false;
}
function isTimeoutError(err, nested = false) {
	if (err === void 0 || err === null) return false;
	if (err.name == "TimeoutError") return true;
	if (err.name == "RpcError" && err.code == "DEADLINE_EXCEEDED") return true;
	if (err.name == "RESTError" && err.status.code == require_code.Code.DEADLINE_EXCEEDED) return true;
	if (err.cause !== void 0 && !nested) return isTimeoutError(err.cause, true);
	return false;
}
function isCancelError(err, nested = false) {
	if (err === void 0 || err === null) return false;
	if (err.name == "RpcError" && err.code == "CANCELLED") return true;
	if (err.name == "RESTError" && err.status.code == require_code.Code.CANCELLED) return true;
	if (err.cause !== void 0 && !nested) return isCancelError(err.cause, true);
	return false;
}
function isAbortedError(err, nested = false) {
	if (err === void 0 || err === null) return false;
	if (err instanceof _milaboratories_ts_helpers.Aborted || err.name == "AbortError") return true;
	if (err.code == "ABORT_ERR") return true;
	if (err instanceof DOMException && err.code === DOMException.ABORT_ERR) return true;
	if (err.name == "RpcError" && err.code == "ABORTED") return true;
	if (err.name == "RESTError" && err.status.code == require_code.Code.ABORTED) return true;
	if (err.cause !== void 0 && !nested) return isAbortedError(err.cause, true);
	return false;
}
function isTimeoutOrCancelError(err, nested = false) {
	if (err === void 0 || err === null) return false;
	if (isAbortedError(err, true)) return true;
	if (isTimeoutError(err, true)) return true;
	if (isCancelError(err, true)) return true;
	if (err.cause !== void 0 && !nested) return isTimeoutOrCancelError(err.cause, true);
	return false;
}
function isUnimplementedError(err, nested = false) {
	if (err === void 0 || err === null) return false;
	if (err.name == "RpcError" && err.code == "UNIMPLEMENTED") return true;
	if (err.name == "RESTError" && err.status.code == require_code.Code.UNIMPLEMENTED) return true;
	if (err.cause !== void 0 && !nested) return isUnimplementedError(err.cause, true);
	return false;
}
function isNotFoundError(err, nested = false) {
	if (err === void 0 || err === null) return false;
	if (err.name == "RpcError" && err.code == "NOT_FOUND") return true;
	if (err.name == "RESTError" && err.status.code == require_code.Code.NOT_FOUND) return true;
	if (err.cause !== void 0 && !nested) return isNotFoundError(err.cause, true);
	return err instanceof RecoverablePlError && err.status.code === PlErrorCodeNotFound;
}
const PlErrorCodeNotFound = require_code.Code.NOT_FOUND;
var PlError = class extends Error {
	name = "PlError";
	constructor(status, opts) {
		super(`code=${status.code} ${status.message}`, opts);
		this.status = status;
	}
};
function throwPlNotFoundError(message) {
	throw new RecoverablePlError({
		code: PlErrorCodeNotFound,
		message,
		details: []
	});
}
var RecoverablePlError = class extends PlError {
	name = "RecoverablePlError";
	constructor(status) {
		super(status);
	}
};
var UnrecoverablePlError = class extends PlError {
	name = "UnrecoverablePlError";
	constructor(status) {
		super(status);
	}
};
var UnauthenticatedError = class extends Error {
	name = "UnauthenticatedError";
	constructor(message) {
		super("LoginFailed: " + message);
	}
};
var DisconnectedError = class extends Error {
	name = "DisconnectedError";
	constructor(message) {
		super("Disconnected: " + message);
	}
};
var RESTError = class extends PlError {
	name = "RESTError";
	constructor(status, opts) {
		super(status, opts);
	}
};
function rethrowMeaningfulError(error, wrapIfUnknown = false) {
	if (isUnauthenticated(error)) {
		if (error instanceof UnauthenticatedError) throw error;
		throw new UnauthenticatedError(error.message);
	}
	if (isConnectionProblem(error)) {
		if (error instanceof DisconnectedError) throw error;
		throw new DisconnectedError(error.message);
	}
	if (isTimeoutOrCancelError(error)) throw new _milaboratories_ts_helpers.Aborted(error);
	if (wrapIfUnknown) {
		const message = error.message || String(error) || "Unknown error";
		throw new Error(message, { cause: error });
	} else throw error;
}
//#endregion
exports.DisconnectedError = DisconnectedError;
exports.PlError = PlError;
exports.PlErrorCodeNotFound = PlErrorCodeNotFound;
exports.RESTError = RESTError;
exports.RecoverablePlError = RecoverablePlError;
exports.UnauthenticatedError = UnauthenticatedError;
exports.UnrecoverablePlError = UnrecoverablePlError;
exports.isAbortedError = isAbortedError;
exports.isCancelError = isCancelError;
exports.isConnectionProblem = isConnectionProblem;
exports.isNotFoundError = isNotFoundError;
exports.isTimeoutError = isTimeoutError;
exports.isTimeoutOrCancelError = isTimeoutOrCancelError;
exports.isUnauthenticated = isUnauthenticated;
exports.isUnimplementedError = isUnimplementedError;
exports.rethrowMeaningfulError = rethrowMeaningfulError;
exports.throwPlNotFoundError = throwPlNotFoundError;

//# sourceMappingURL=errors.cjs.map