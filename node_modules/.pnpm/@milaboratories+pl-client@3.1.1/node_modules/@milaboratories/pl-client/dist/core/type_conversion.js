import "./types.js";
import { FieldType } from "../proto-grpc/github.com/milaboratory/pl/plapi/plapiproto/base_types.js";
import { Field_ValueStatus, Resource_Kind } from "../proto-grpc/github.com/milaboratory/pl/plapi/plapiproto/api_types.js";
import { throwPlNotFoundError } from "./errors.js";
import { assertNever, notEmpty } from "@milaboratories/ts-helpers";
//#region src/core/type_conversion.ts
const ResourceErrorField = "resourceError";
function resourceIsDeleted(proto) {
	return proto.deletedTime !== void 0 && proto.deletedTime.seconds !== 0n;
}
/** Throws "native" pl not found error, if resource is marked as deleted. */
function protoToResource(proto) {
	if (resourceIsDeleted(proto)) throwPlNotFoundError("resource deleted");
	return {
		id: proto.resourceId,
		originalResourceId: proto.originalResourceId,
		type: notEmpty(proto.type),
		data: proto.data,
		inputsLocked: proto.inputsLocked,
		outputsLocked: proto.outputsLocked,
		resourceReady: proto.resourceReady,
		kind: protoToResourceKind(proto.kind),
		error: protoToError(proto),
		final: proto.isFinal,
		fields: proto.fields?.filter((f) => f.id.fieldName !== ResourceErrorField).map(protoToField)
	};
}
function protoToResourceKind(proto) {
	switch (proto) {
		case Resource_Kind.STRUCTURAL: return "Structural";
		case Resource_Kind.VALUE: return "Value";
	}
	throw new Error("invalid ResourceKind: " + proto);
}
function protoToError(proto) {
	return proto.fields.find((f) => f?.id?.fieldName === ResourceErrorField)?.error ?? 0n;
}
function protoToField(proto) {
	return {
		name: notEmpty(proto.id?.fieldName),
		type: protoToFieldType(proto.type),
		status: protoToFieldStatus(proto.valueStatus),
		value: proto.value,
		error: proto.error,
		valueIsFinal: proto.valueIsFinal
	};
}
function protoToFieldType(proto) {
	switch (proto) {
		case FieldType.INPUT: return "Input";
		case FieldType.OUTPUT: return "Output";
		case FieldType.SERVICE: return "Service";
		case FieldType.ONE_TIME_WRITABLE: return "OTW";
		case FieldType.DYNAMIC: return "Dynamic";
		case FieldType.MULTIPLE_TIMES_WRITABLE: return "MTW";
		default: throw new Error("invalid FieldType: " + proto);
	}
}
function protoToFieldStatus(proto) {
	switch (proto) {
		case Field_ValueStatus.EMPTY: return "Empty";
		case Field_ValueStatus.ASSIGNED: return "Assigned";
		case Field_ValueStatus.RESOLVED: return "Resolved";
		default: throw new Error("invalid FieldStatus: " + proto);
	}
}
function fieldTypeToProto(type) {
	switch (type) {
		case "Input": return FieldType.INPUT;
		case "Output": return FieldType.OUTPUT;
		case "Dynamic": return FieldType.DYNAMIC;
		case "Service": return FieldType.SERVICE;
		case "MTW": return FieldType.MULTIPLE_TIMES_WRITABLE;
		case "OTW": return FieldType.ONE_TIME_WRITABLE;
		default: return assertNever(type);
	}
}
//#endregion
export { fieldTypeToProto, protoToField, protoToResource };

//# sourceMappingURL=type_conversion.js.map