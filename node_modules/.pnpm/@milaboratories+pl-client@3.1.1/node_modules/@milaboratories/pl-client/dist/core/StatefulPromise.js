//#region src/core/StatefulPromise.ts
/**
* A Promise wrapper that tracks its state (pending/fulfilled/rejected) and provides
* optional deferred rejection handling to prevent unhandled promise rejections.
*/
var StatefulPromise = class StatefulPromise {
	_state;
	_id;
	static idCounter = 0n;
	static debug = false;
	static from(promise, options = {}) {
		return new StatefulPromise(promise, options);
	}
	static fromDeferredReject(promise, onUnwrap) {
		return new StatefulPromise(promise, {
			deferReject: true,
			onUnwrap
		});
	}
	static fromDeferredRejectCallback(asyncFn, onUnwrap) {
		return new StatefulPromise(asyncFn(), {
			deferReject: true,
			onUnwrap
		});
	}
	constructor(promise, options = {}) {
		this.promise = promise;
		this.options = options;
		this._state = "pending";
		this._id = StatefulPromise.idCounter++;
		this.promise.then((value) => {
			this._state = "fulfilled";
			return value;
		}).catch((err) => {
			this._state = "rejected";
			if (!options.deferReject) throw err;
		});
	}
	[Symbol.toStringTag] = "Promise";
	get id() {
		return this._id;
	}
	get state() {
		return this._state;
	}
	then(onfulfilled, onrejected) {
		return this.unwrap().then(onfulfilled, onrejected);
	}
	catch(onrejected) {
		return this.unwrap().catch(onrejected);
	}
	finally(onfinally) {
		return this.unwrap().finally(onfinally);
	}
	async unwrap() {
		if (this.options.onUnwrap) this.options.onUnwrap(this);
		return this.promise;
	}
};
//#endregion
export { StatefulPromise };

//# sourceMappingURL=StatefulPromise.js.map