Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
let undici = require("undici");

//#region src/index.ts
/**
* Creates default HTTP dispatcher that uses given proxy settings.
* @param httpProxy - Proxy settings to use for HTTP requests.
* @param customInterceptors - list of interceptors to be applied instead of default.
*                             NOTE: DNS cache always gets applied when proxy is not used.
* @returns Dispatcher for HTTP requests.
* @see {@link https://undici.nodejs.org/#/docs/api/Dispatcher?id=dispatchercomposeinterceptors-interceptor}
*/
function defaultHttpDispatcher(httpProxy, customInterceptors) {
	const httpOptions = {
		autoSelectFamily: false,
		headersTimeout: 15e3,
		bodyTimeout: 3e4,
		keepAliveTimeout: 15e3,
		keepAliveMaxTimeout: 6e4
	};
	const proxy = typeof httpProxy === "string" ? { url: httpProxy } : httpProxy;
	const dispatcher = proxy?.url ? new undici.ProxyAgent({
		uri: proxy.url,
		token: proxy.auth,
		...httpOptions
	}) : new undici.Agent(httpOptions).compose(undici.interceptors.dns({
		maxTTL: 6e4,
		affinity: 4
	}));
	const appliedInterceptors = customInterceptors ?? [undici.interceptors.retry()];
	return dispatcher.compose(...appliedInterceptors);
}

//#endregion
exports.defaultHttpDispatcher = defaultHttpDispatcher;
//# sourceMappingURL=index.cjs.map