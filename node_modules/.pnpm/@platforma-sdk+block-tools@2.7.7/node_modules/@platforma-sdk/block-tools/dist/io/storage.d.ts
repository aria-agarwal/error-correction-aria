import { S3 } from '@aws-sdk/client-s3';
export interface RegistryStorage {
    putFile(file: string, buffer: Buffer): Promise<void>;
    getFile(file: string): Promise<Buffer | undefined>;
    listFiles(prefix: string): Promise<string[]>;
    deleteFiles(...files: string[]): Promise<void>;
}
export declare class S3Storage implements RegistryStorage {
    readonly client: S3;
    readonly bucket: string;
    readonly root: string;
    constructor(client: S3, bucket: string, root: string);
    getFile(file: string): Promise<Buffer | undefined>;
    listFiles(prefix: string): Promise<string[]>;
    putFile(file: string, buffer: Buffer): Promise<void>;
    deleteFiles(...files: string[]): Promise<void>;
}
export declare class FSStorage implements RegistryStorage {
    /** Absolute path */
    readonly root: string;
    constructor(_root: string);
    private toAbsolutePath;
    getFile(address: string): Promise<Buffer | undefined>;
    listFiles(prefix: string): Promise<string[]>;
    putFile(address: string, buffer: Buffer): Promise<void>;
    deleteFiles(...files: string[]): Promise<void>;
}
export declare function storageByUrl(address: string): RegistryStorage;
