import { CLIError, artifactIDToString, assertNever, currentArch, currentPlatform, ensureDirsExist, hashDirMetaSync, hashDirSync, urlJoin } from "./util.js";
import { CONDA_FROZEN_ENV_SPEC_FILE, PYTHON_TOOLSET } from "../defaults.js";
import { isCrossPlatform } from "./schemas/artifacts.js";
import { entrypointNameToOrigin } from "./docker.js";
import { descriptorFilePath, resolveRunEnvironment } from "./resolver.js";
import path from "node:path";
import fs from "node:fs";

//#region src/core/sw-json-render.ts
var SwJsonRenderer = class {
	constructor(logger, pkgInfo) {
		this.logger = logger;
		this.pkgInfo = pkgInfo;
	}
	renderSoftwareEntrypoints(mode, entrypoints, options) {
		const result = /* @__PURE__ */ new Map();
		let hasReferences = false;
		const fullDirHash = options?.fullDirHash ?? false;
		this.logger.info(`Rendering entrypoint descriptors...`);
		this.logger.debug("  entrypoints: " + JSON.stringify(entrypoints));
		for (const [epName, ep] of entrypoints.entries()) {
			if (ep.type === "reference") {
				hasReferences = true;
				continue;
			}
			this.logger.debug(`Rendering entrypoint descriptor '${epName}'...`);
			const originEpName = entrypointNameToOrigin(epName);
			const info = result.has(originEpName) ? result.get(originEpName) : { id: {
				package: this.pkgInfo.packageName,
				name: originEpName
			} };
			if (!info) throw CLIError(`Entrypoint ${epName} not found in result`);
			if (mode !== "release") info.isDev = true;
			const pkg = ep.artifact;
			if (mode === "dev-local") {
				switch (pkg.type) {
					case "docker":
						info.docker = this.renderDockerInfo(epName, ep, options?.requireAllArtifacts);
						break;
					default:
						this.logger.debug("  rendering 'local' source...");
						info.local = this.renderLocalPackage(epName, ep, fullDirHash);
				}
				result.set(originEpName, info);
				continue;
			}
			const type = pkg.type;
			switch (type) {
				case "R":
					info.binary = this.renderBinaryInfo(mode, epName, ep, options?.requireAllArtifacts);
					break;
				case "environment":
					info.runEnv = this.renderRunEnvInfo(mode, epName, ep, options?.requireAllArtifacts);
					break;
				case "asset":
					info.asset = this.renderAssetInfo(mode, epName, ep, options?.requireAllArtifacts);
					break;
				case "binary":
					info.binary = this.renderBinaryInfo(mode, epName, ep, options?.requireAllArtifacts);
					break;
				case "docker":
					info.docker = this.renderDockerInfo(epName, ep, options?.requireAllArtifacts);
					break;
				case "java":
					info.binary = this.renderBinaryInfo(mode, epName, ep, options?.requireAllArtifacts);
					break;
				case "python":
					info.binary = this.renderBinaryInfo(mode, epName, ep, options?.requireAllArtifacts);
					break;
				case "conda":
					info.binary = this.renderBinaryInfo(mode, epName, ep, options?.requireAllArtifacts);
					break;
				default:
					assertNever(type);
					throw new Error(`renderer logic error: renderSoftwareEntrypoints does not cover all package types`);
			}
			result.set(originEpName, info);
		}
		if (result.size === 0 && !hasReferences) {
			this.logger.error("no entrypoint descriptors were rendered");
			throw CLIError("no entrypoint descriptors were rendered");
		}
		return result;
	}
	writeSwJson(info, dstFile) {
		const epType = info.asset ? "asset" : "software";
		const dstSwInfoPath = dstFile ?? descriptorFilePath(this.pkgInfo.packageRoot, epType, info.id.name);
		this.logger.info(`Writing entrypoint descriptor to '${dstSwInfoPath}'`);
		const { id, ...toEncode } = info;
		const encoded = JSON.stringify({
			name: artifactIDToString(id),
			...toEncode
		});
		ensureDirsExist(path.dirname(dstSwInfoPath));
		fs.writeFileSync(dstSwInfoPath, encoded + "\n");
	}
	copySwJson(epName, srcFile) {
		let epType;
		if (srcFile.endsWith(compiledSoftwareSuffix)) epType = "software";
		else if (srcFile.endsWith(compiledAssetSuffix)) epType = "asset";
		else throw CLIError(`unknown entrypoint '${epName}' type: cannot get type from extension of source file ${srcFile}`);
		const dstSwInfoPath = descriptorFilePath(this.pkgInfo.packageRoot, epType, epName);
		ensureDirsExist(path.dirname(dstSwInfoPath));
		fs.copyFileSync(srcFile, dstSwInfoPath);
	}
	renderLocalPackage(epName, ep, fullDirHash) {
		const artifact = ep.artifact;
		const rootDir = this.pkgInfo.artifactContentRoot(artifact, currentPlatform());
		const hash = fullDirHash ? hashDirSync(rootDir) : hashDirMetaSync(rootDir);
		const epType = ep.type;
		switch (epType) {
			case "environment": throw CLIError(`entrypoint ${epName} points to 'environment' artifact, which does not support local build yet`);
			case "asset": throw CLIError(`entrypoint ${epName} points to 'asset' artifact, which does not support local build yet`);
			case "software": {
				const pkgType = artifact.type;
				switch (pkgType) {
					case "environment": throw CLIError(`entrypoint is incompatible with artifact type: ep=${epName} (software), artifact='${pkgType}'`);
					case "asset": throw CLIError(`entrypoint is incompatible with artifact type: ep=${epName} (software), artifact='${pkgType}'`);
					case "binary": return {
						type: "binary",
						hash: hash.digest().toString("hex"),
						path: rootDir,
						cmd: ep.cmd,
						envVars: ep.env
					};
					case "java": return {
						type: "java",
						hash: hash.digest().toString("hex"),
						path: rootDir,
						cmd: ep.cmd,
						envVars: ep.env,
						runEnv: resolveRunEnvironment(this.logger, this.pkgInfo.packageRoot, this.pkgInfo.packageName, artifact.environment, artifact.type)
					};
					case "python": {
						const { toolset, ...deps } = artifact.dependencies ?? {};
						return {
							type: "python",
							hash: hash.digest().toString("hex"),
							path: rootDir,
							cmd: ep.cmd,
							envVars: ep.env,
							runEnv: resolveRunEnvironment(this.logger, this.pkgInfo.packageRoot, this.pkgInfo.packageName, artifact.environment, artifact.type),
							toolset: toolset ?? PYTHON_TOOLSET,
							dependencies: deps
						};
					}
					case "R": return {
						type: "R",
						hash: hash.digest().toString("hex"),
						path: rootDir,
						cmd: ep.cmd,
						envVars: ep.env,
						toolset: "renv",
						dependencies: {},
						runEnv: resolveRunEnvironment(this.logger, this.pkgInfo.packageRoot, this.pkgInfo.packageName, artifact.environment, artifact.type)
					};
					case "docker": throw new Error(`internal build script logic error: attempt to build docker entrypoint as local binary package`);
					case "conda": return {
						type: "conda",
						hash: hash.digest().toString("hex"),
						path: rootDir,
						cmd: ep.cmd,
						envVars: ep.env,
						["micromamba-version"]: artifact["micromamba-version"],
						["conda-root-dir"]: artifact["conda-root-dir"],
						spec: artifact.spec
					};
					default:
						assertNever(pkgType);
						throw new Error("renderer logic error: renderLocalInfo does not cover all artifact types");
				}
			}
			default:
				assertNever(epType);
				throw new Error("renderer logic error: renderLocalInfo does not cover all environment types");
		}
	}
	renderBinaryInfo(mode, epName, ep, requireArtifactInfo) {
		switch (mode) {
			case "release": break;
			case "dev-local": throw new Error(`'*.sw.json' generator logic error`);
			default: assertNever(mode);
		}
		ep = requireEntrypointType(ep, "software", `internal build script logic error`);
		const binPkg = ep.artifact;
		const artInfoPath = this.pkgInfo.artifactInfoLocation(binPkg.id, "archive", isCrossPlatform(binPkg.type) ? void 0 : currentPlatform());
		const artInfo = readArtifactInfoIfExists(artInfoPath, epName, requireArtifactInfo);
		if (!artInfo) return;
		if (!artInfo.registryName) throw CLIError(`could not render binary entrypoint '${epName}': registry name is not set in artifact info file ${artInfoPath}`);
		const pkgType = binPkg.type;
		switch (pkgType) {
			case "docker": throw new Error(`internal build script logic error: entrypoint is incompatible with artifact type: ep=${epName} (software), artifact='${pkgType}'`);
			case "conda": return {
				type: "conda",
				registry: artInfo.registryName,
				package: artInfo.remoteArtifactLocation,
				cmd: ep.cmd,
				envVars: ep.env,
				["micromamba-version"]: binPkg["micromamba-version"],
				["conda-root-dir"]: binPkg["conda-root-dir"],
				spec: CONDA_FROZEN_ENV_SPEC_FILE
			};
			case "binary": return {
				type: "binary",
				registry: artInfo.registryName,
				package: artInfo.remoteArtifactLocation,
				cmd: ep.cmd,
				envVars: ep.env
			};
			case "java": return {
				type: "java",
				registry: artInfo.registryName,
				package: artInfo.remoteArtifactLocation,
				cmd: ep.cmd,
				envVars: ep.env,
				runEnv: resolveRunEnvironment(this.logger, this.pkgInfo.packageRoot, this.pkgInfo.packageName, binPkg.environment, binPkg.type)
			};
			case "python": {
				const { toolset, ...deps } = binPkg.dependencies ?? {};
				return {
					type: "python",
					registry: artInfo.registryName,
					package: artInfo.remoteArtifactLocation,
					cmd: ep.cmd,
					envVars: ep.env,
					runEnv: resolveRunEnvironment(this.logger, this.pkgInfo.packageRoot, this.pkgInfo.packageName, binPkg.environment, binPkg.type),
					toolset: toolset ?? "pip",
					dependencies: deps
				};
			}
			case "R": return {
				type: "R",
				registry: artInfo.registryName,
				package: artInfo.remoteArtifactLocation,
				cmd: ep.cmd,
				envVars: ep.env,
				runEnv: resolveRunEnvironment(this.logger, this.pkgInfo.packageRoot, this.pkgInfo.packageName, binPkg.environment, binPkg.type),
				toolset: "renv",
				dependencies: {}
			};
			default:
				assertNever(pkgType);
				throw new Error("internal build script logic error: renderBinaryInfo does not cover all package types");
		}
	}
	renderRunEnvInfo(mode, epName, ep, requireArtifactInfo) {
		switch (mode) {
			case "release": break;
			case "dev-local": throw CLIError(`run environments do not support 'local' dev build mode yet`);
			default: assertNever(mode);
		}
		ep = requireEntrypointType(ep, "environment", `internal build script logic error`);
		const envArtifact = requireArtifactType(ep.artifact, "environment", `could not render 'environment' entrypoint`);
		const artInfo = readArtifactInfoIfExists(this.pkgInfo.artifactInfoLocation(envArtifact.id, "archive", currentPlatform()), epName, requireArtifactInfo);
		if (!artInfo) return;
		return {
			type: envArtifact.runtime,
			["r-version"]: envArtifact["r-version"],
			["python-version"]: envArtifact["python-version"],
			["java-version"]: envArtifact["java-version"],
			envVars: envArtifact.envVars ?? [],
			registry: artInfo.registryName,
			package: artInfo.remoteArtifactLocation,
			binDir: envArtifact.binDir
		};
	}
	renderAssetInfo(mode, epName, ep, requireArtifactInfo) {
		switch (mode) {
			case "release": break;
			case "dev-local": throw CLIError(`assets do not support 'local' dev build mode yet`);
			default: assertNever(mode);
		}
		ep = requireEntrypointType(ep, "asset", `internal build script logic error`);
		const assetPkg = requireArtifactType(ep.artifact, "asset", `could not render 'asset' entrypoint`);
		if (assetPkg.type !== "asset") throw CLIError(`could not render asset entrypoint '${epName}': not 'asset' artifact`);
		const artInfo = readArtifactInfoIfExists(this.pkgInfo.artifactInfoLocation(assetPkg.id, "archive", void 0), epName, requireArtifactInfo);
		if (!artInfo) return;
		if (!artInfo.registryURL) throw CLIError(`could not render asset entrypoint '${epName}': base download URL is not configured for asset's registry`);
		return {
			registry: artInfo.registryName,
			package: artInfo.remoteArtifactLocation,
			url: urlJoin(artInfo.registryURL, artInfo.remoteArtifactLocation)
		};
	}
	renderDockerInfo(epName, ep, requireArtifactInfo) {
		ep = requireEntrypointType(ep, "software", `internal build script logic error`);
		const dockerArtifact = requireArtifactType(ep.artifact, "docker", `could not render 'docker' entrypoint`);
		const artInfo = readArtifactInfoIfExists(this.pkgInfo.artifactInfoLocation(dockerArtifact.id, "docker", currentArch()), epName, requireArtifactInfo);
		if (!artInfo) return;
		return {
			tag: artInfo.remoteArtifactLocation,
			entrypoint: artInfo.entrypoint ?? [],
			cmd: ep.cmd,
			pkg: dockerArtifact.pkg || "/"
		};
	}
};
const compiledSoftwareSuffix = ".sw.json";
const compiledAssetSuffix = ".as.json";
function writeBuiltArtifactInfo(location, locInfo) {
	fs.mkdirSync(path.dirname(location), { recursive: true });
	fs.writeFileSync(location, JSON.stringify(locInfo), { encoding: "utf8" });
}
function readBuiltArtifactInfo(location) {
	const data = fs.readFileSync(location, "utf8");
	return JSON.parse(data);
}
function readArtifactInfoIfExists(location, epName, requireExisting) {
	if (fs.existsSync(location)) return readBuiltArtifactInfo(location);
	if (requireExisting) throw CLIError(`could not render docker entrypoint '${epName}': artifact info file '${location}' does not exist`);
}
function requireEntrypointType(ep, type, errMsg) {
	if (ep.type === type) return ep;
	throw new Error(`${errMsg}: entrypoint ${ep.name} (${ep.type}) is not '${type}'`);
}
function requireArtifactType(pkg, type, errMsg) {
	if (pkg.type === type) return pkg;
	if (errMsg) errMsg = `${errMsg}: `;
	throw CLIError(`${errMsg}artifact '${pkg.id}' ('${pkg.type}') is not '${type}'`);
}

//#endregion
export { SwJsonRenderer, readBuiltArtifactInfo, writeBuiltArtifactInfo };
//# sourceMappingURL=sw-json-render.js.map