const require_runtime = require('../../_virtual/_rolldown/runtime.cjs');
const require_util = require('../util.cjs');
const require_defaults = require('../../defaults.cjs');
const require_artifacts = require('./artifacts.cjs');
let zod_v4 = require("zod/v4");

//#region src/core/schemas/sw-json.ts
const remoteLocationSchema = zod_v4.z.object({
	registry: zod_v4.z.string().describe("name of the registry to use for package download"),
	package: zod_v4.z.string().describe("full package path in registry, e.g. 'common/jdk/21.0.2.13.1-{os}-{arch}.tgz")
});
const assetSchema = zod_v4.z.object({
	...remoteLocationSchema.shape,
	url: zod_v4.z.string().describe("asset download URL")
});
const dockerSchema = zod_v4.z.object({
	tag: zod_v4.z.string().describe("full image tag to pull on backend side to execute this software"),
	entrypoint: zod_v4.z.array(zod_v4.z.string()).describe("override image's entrypoint"),
	cmd: zod_v4.z.array(zod_v4.z.string()).describe("command to be run in the container"),
	pkg: zod_v4.z.string().default(require_defaults.DOCKER_PLACEHOLDER_PKG).describe("what to substitute in place of \"{pkg}\" variable in \"cmd\" (for artifacts with docker autogeneration)")
});
const runEnvironmentSchema = zod_v4.z.object({
	type: zod_v4.z.enum(require_artifacts.runEnvironmentTypes),
	...remoteLocationSchema.shape,
	["r-version"]: zod_v4.z.string().optional(),
	["python-version"]: zod_v4.z.string().optional(),
	["java-version"]: zod_v4.z.string().optional(),
	envVars: zod_v4.z.array(zod_v4.z.string().regex(/=/, "environment variable should be specified in format: <var-name>=<var-value>, i.e.: MY_ENV=value")).optional(),
	binDir: zod_v4.z.string()
});
const runDependencyJavaSchema = runEnvironmentSchema.extend({
	type: zod_v4.z.literal("java"),
	name: zod_v4.z.string().describe("name used to import this package as software dependency of tengo script")
});
const runDependencyPythonSchema = runEnvironmentSchema.extend({
	type: zod_v4.z.literal("python"),
	["python-version"]: zod_v4.z.string(),
	name: zod_v4.z.string().describe("name used to import this package as software dependency of tengo script")
});
const runDependencyRSchema = runEnvironmentSchema.extend({
	type: zod_v4.z.literal("R"),
	["r-version"]: zod_v4.z.string(),
	name: zod_v4.z.string().describe("name used to import this package as software dependency of tengo script")
});
const commonPackageSettingsSchema = zod_v4.z.object({
	cmd: zod_v4.z.array(zod_v4.z.string()).min(1).describe("run given command, appended by args from workflow"),
	envVars: zod_v4.z.array(zod_v4.z.string().regex(/=/, "full environment variable specification is required: <var-name>=<var-value>, e.g.: IS_CI=yes")).optional()
});
const binaryPackageSchema = zod_v4.z.object({
	type: zod_v4.z.literal("binary"),
	...commonPackageSettingsSchema.shape,
	pkg: zod_v4.z.string().optional().describe("location of all package contents in Docker container (default: /app)")
});
const javaPackageSettingsSchema = zod_v4.z.object({
	type: zod_v4.z.literal("java"),
	...commonPackageSettingsSchema.shape,
	runEnv: runDependencyJavaSchema
});
const pythonPackageSettingsSchema = zod_v4.z.object({
	type: zod_v4.z.literal("python"),
	...commonPackageSettingsSchema.shape,
	runEnv: runDependencyPythonSchema,
	toolset: zod_v4.z.string(),
	dependencies: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.string()).describe("paths of files that describe dependencies for given toolset: say, requirements.txt for 'pip'")
});
const rPackageSettingsSchema = zod_v4.z.object({
	type: zod_v4.z.literal("R"),
	...commonPackageSettingsSchema.shape,
	runEnv: runDependencyRSchema,
	toolset: zod_v4.z.string(),
	dependencies: zod_v4.z.record(zod_v4.z.string(), zod_v4.z.string()).describe("paths of files that describe dependencies for given toolset: say, requirements.txt for 'pip'")
});
const condaPackageSchema = zod_v4.z.object({
	type: zod_v4.z.literal("conda"),
	...commonPackageSettingsSchema.shape,
	["micromamba-version"]: zod_v4.z.string().describe("version of micromamba to be used to operate with conda environments"),
	["conda-root-dir"]: zod_v4.z.string().default(require_defaults.CONDA_DATA_LOCATION).describe("root directory of conda environment inside package root"),
	spec: zod_v4.z.string().describe("location of spec.yaml describing conda environment, relative to package root.")
});
const remoteSoftwareSchema = zod_v4.z.union([
	remoteLocationSchema.extend(binaryPackageSchema.shape),
	remoteLocationSchema.extend(javaPackageSettingsSchema.shape),
	remoteLocationSchema.extend(pythonPackageSettingsSchema.shape),
	remoteLocationSchema.extend(rPackageSettingsSchema.shape),
	remoteLocationSchema.extend(condaPackageSchema.shape)
]);
const localLocationSchema = zod_v4.z.object({
	hash: zod_v4.z.string().describe("hash of software directory. Makes deduplication to work properly when you actively develop software"),
	path: zod_v4.z.string().describe("absolute path to root directory of software on local host")
});
const localSoftwareSchema = zod_v4.z.discriminatedUnion("type", [
	localLocationSchema.extend(binaryPackageSchema.shape),
	localLocationSchema.extend(javaPackageSettingsSchema.shape),
	localLocationSchema.extend(pythonPackageSettingsSchema.shape),
	localLocationSchema.extend(rPackageSettingsSchema.shape),
	localLocationSchema.extend(condaPackageSchema.shape)
]);
const swJsonSchema = zod_v4.z.object({
	isDev: zod_v4.z.boolean().optional(),
	asset: assetSchema.optional(),
	binary: remoteSoftwareSchema.optional(),
	docker: dockerSchema.optional(),
	runEnv: runEnvironmentSchema.optional(),
	local: localSoftwareSchema.optional()
}).refine((data) => require_util.toInt(data.runEnv) + require_util.toInt(data.binary || data.docker) + require_util.toInt(data.asset) + require_util.toInt(data.local) == 1, {
	message: "entrypoint cannot point to several packages at once: choose 'environment', 'binary', 'asset', 'conda' or 'local'",
	path: ["environment | binary | asset | conda | local"]
});

//#endregion
exports.swJsonSchema = swJsonSchema;
//# sourceMappingURL=sw-json.cjs.map