import { toInt } from "../util.js";
import { CONDA_DATA_LOCATION, DOCKER_PLACEHOLDER_PKG } from "../../defaults.js";
import { runEnvironmentTypes } from "./artifacts.js";
import { z } from "zod/v4";

//#region src/core/schemas/sw-json.ts
const remoteLocationSchema = z.object({
	registry: z.string().describe("name of the registry to use for package download"),
	package: z.string().describe("full package path in registry, e.g. 'common/jdk/21.0.2.13.1-{os}-{arch}.tgz")
});
const assetSchema = z.object({
	...remoteLocationSchema.shape,
	url: z.string().describe("asset download URL")
});
const dockerSchema = z.object({
	tag: z.string().describe("full image tag to pull on backend side to execute this software"),
	entrypoint: z.array(z.string()).describe("override image's entrypoint"),
	cmd: z.array(z.string()).describe("command to be run in the container"),
	pkg: z.string().default(DOCKER_PLACEHOLDER_PKG).describe("what to substitute in place of \"{pkg}\" variable in \"cmd\" (for artifacts with docker autogeneration)")
});
const runEnvironmentSchema = z.object({
	type: z.enum(runEnvironmentTypes),
	...remoteLocationSchema.shape,
	["r-version"]: z.string().optional(),
	["python-version"]: z.string().optional(),
	["java-version"]: z.string().optional(),
	envVars: z.array(z.string().regex(/=/, "environment variable should be specified in format: <var-name>=<var-value>, i.e.: MY_ENV=value")).optional(),
	binDir: z.string()
});
const runDependencyJavaSchema = runEnvironmentSchema.extend({
	type: z.literal("java"),
	name: z.string().describe("name used to import this package as software dependency of tengo script")
});
const runDependencyPythonSchema = runEnvironmentSchema.extend({
	type: z.literal("python"),
	["python-version"]: z.string(),
	name: z.string().describe("name used to import this package as software dependency of tengo script")
});
const runDependencyRSchema = runEnvironmentSchema.extend({
	type: z.literal("R"),
	["r-version"]: z.string(),
	name: z.string().describe("name used to import this package as software dependency of tengo script")
});
const commonPackageSettingsSchema = z.object({
	cmd: z.array(z.string()).min(1).describe("run given command, appended by args from workflow"),
	envVars: z.array(z.string().regex(/=/, "full environment variable specification is required: <var-name>=<var-value>, e.g.: IS_CI=yes")).optional()
});
const binaryPackageSchema = z.object({
	type: z.literal("binary"),
	...commonPackageSettingsSchema.shape,
	pkg: z.string().optional().describe("location of all package contents in Docker container (default: /app)")
});
const javaPackageSettingsSchema = z.object({
	type: z.literal("java"),
	...commonPackageSettingsSchema.shape,
	runEnv: runDependencyJavaSchema
});
const pythonPackageSettingsSchema = z.object({
	type: z.literal("python"),
	...commonPackageSettingsSchema.shape,
	runEnv: runDependencyPythonSchema,
	toolset: z.string(),
	dependencies: z.record(z.string(), z.string()).describe("paths of files that describe dependencies for given toolset: say, requirements.txt for 'pip'")
});
const rPackageSettingsSchema = z.object({
	type: z.literal("R"),
	...commonPackageSettingsSchema.shape,
	runEnv: runDependencyRSchema,
	toolset: z.string(),
	dependencies: z.record(z.string(), z.string()).describe("paths of files that describe dependencies for given toolset: say, requirements.txt for 'pip'")
});
const condaPackageSchema = z.object({
	type: z.literal("conda"),
	...commonPackageSettingsSchema.shape,
	["micromamba-version"]: z.string().describe("version of micromamba to be used to operate with conda environments"),
	["conda-root-dir"]: z.string().default(CONDA_DATA_LOCATION).describe("root directory of conda environment inside package root"),
	spec: z.string().describe("location of spec.yaml describing conda environment, relative to package root.")
});
const remoteSoftwareSchema = z.union([
	remoteLocationSchema.extend(binaryPackageSchema.shape),
	remoteLocationSchema.extend(javaPackageSettingsSchema.shape),
	remoteLocationSchema.extend(pythonPackageSettingsSchema.shape),
	remoteLocationSchema.extend(rPackageSettingsSchema.shape),
	remoteLocationSchema.extend(condaPackageSchema.shape)
]);
const localLocationSchema = z.object({
	hash: z.string().describe("hash of software directory. Makes deduplication to work properly when you actively develop software"),
	path: z.string().describe("absolute path to root directory of software on local host")
});
const localSoftwareSchema = z.discriminatedUnion("type", [
	localLocationSchema.extend(binaryPackageSchema.shape),
	localLocationSchema.extend(javaPackageSettingsSchema.shape),
	localLocationSchema.extend(pythonPackageSettingsSchema.shape),
	localLocationSchema.extend(rPackageSettingsSchema.shape),
	localLocationSchema.extend(condaPackageSchema.shape)
]);
const swJsonSchema = z.object({
	isDev: z.boolean().optional(),
	asset: assetSchema.optional(),
	binary: remoteSoftwareSchema.optional(),
	docker: dockerSchema.optional(),
	runEnv: runEnvironmentSchema.optional(),
	local: localSoftwareSchema.optional()
}).refine((data) => toInt(data.runEnv) + toInt(data.binary || data.docker) + toInt(data.asset) + toInt(data.local) == 1, {
	message: "entrypoint cannot point to several packages at once: choose 'environment', 'binary', 'asset', 'conda' or 'local'",
	path: ["environment | binary | asset | conda | local"]
});

//#endregion
export { swJsonSchema };
//# sourceMappingURL=sw-json.js.map