import { isCI } from "../../core/envs.js";
import { createLogger } from "../../core/util.js";
import { BuildFlags, DockerFlags, GlobalFlags, PackageIDFlag, VersionFlag, modeFromFlag, shouldDoAction } from "../../core/cmd-opts.js";
import { Core } from "../../core/core.js";
import { Command } from "@oclif/core";

//#region src/commands/build/docker.ts
var Docker = class Docker extends Command {
	static description = "build docker images";
	static flags = {
		...GlobalFlags,
		...BuildFlags,
		...VersionFlag,
		...PackageIDFlag,
		...DockerFlags
	};
	async run() {
		const { flags } = await this.parse(Docker);
		const core = new Core(createLogger(flags["log-level"]), { packageRoot: flags["package-root"] });
		core.buildMode = modeFromFlag(flags.dev);
		core.pkgInfo.version = flags.version;
		core.buildDockerImages({
			ids: flags["package-id"],
			strictPlatformMatching: isCI()
		});
		if (shouldDoAction(isCI() && !core.pkgInfo.isPrivate, flags["docker-autopush"], flags["docker-no-autopush"]) && !core.pkgInfo.isPrivate) core.publishDockerImages({
			ids: flags["package-id"],
			strictPlatformMatching: isCI()
		});
		core.buildSwJsonFiles({ packageIds: flags["package-id"] ? flags["package-id"] : void 0 });
	}
};

//#endregion
export { Docker as default };
//# sourceMappingURL=docker.js.map