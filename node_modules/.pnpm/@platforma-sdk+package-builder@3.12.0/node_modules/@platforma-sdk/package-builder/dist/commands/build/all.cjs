const require_runtime = require('../../_virtual/_rolldown/runtime.cjs');
const require_envs = require('../../core/envs.cjs');
const require_util = require('../../core/util.cjs');
const require_cmd_opts = require('../../core/cmd-opts.cjs');
const require_core = require('../../core/core.cjs');
let _oclif_core = require("@oclif/core");

//#region src/commands/build/all.ts
var BuildAll = class BuildAll extends _oclif_core.Command {
	static description = "Build all targets (entrypoint descriptors, binary pacakges and so on)";
	static examples = ["<%= config.bin %> <%= command.id %>"];
	static flags = {
		...require_cmd_opts.GlobalFlags,
		...require_cmd_opts.ForceFlag,
		...require_cmd_opts.BuildFlags,
		...require_cmd_opts.PlatformFlags,
		...require_cmd_opts.VersionFlag,
		...require_cmd_opts.DockerFlags,
		...require_cmd_opts.CondaFlags,
		...require_cmd_opts.EntrypointNameFlag,
		...require_cmd_opts.PackageIDFlag,
		...require_cmd_opts.DirHashFlag,
		...require_cmd_opts.ArchiveFlag,
		...require_cmd_opts.ContentRootFlag
	};
	async run() {
		const { flags } = await this.parse(BuildAll);
		const logger = require_util.createLogger(flags["log-level"]);
		try {
			const core = new require_core.Core(logger, { packageRoot: flags["package-root"] });
			core.buildMode = require_cmd_opts.modeFromFlag(flags.dev);
			core.pkgInfo.version = flags.version;
			core.targetPlatform = flags.platform;
			core.allPlatforms = flags["all-platforms"];
			core.fullDirHash = flags["full-dir-hash"];
			const buildDocker = require_cmd_opts.shouldDoAction(require_envs.isCI(), flags["docker-build"], flags["docker-no-build"]);
			if (buildDocker) core.buildDockerImages({
				ids: flags["package-id"],
				registry: flags["docker-registry"],
				strictPlatformMatching: require_envs.isCI()
			});
			await core.buildSoftwareArchives({
				ids: flags["package-id"],
				forceBuild: flags.force,
				archivePath: flags.archive,
				contentRoot: flags["content-root"],
				skipIfEmpty: flags["package-id"] ? false : true,
				condaBuild: require_cmd_opts.shouldDoAction(true, flags["conda-build"], flags["conda-no-build"])
			});
			core.buildSwJsonFiles({ packageIds: flags["package-id"] ? flags["package-id"] : void 0 });
			const autopush = require_cmd_opts.shouldDoAction(require_envs.isCI() && !core.pkgInfo.isPrivate, flags["docker-autopush"], flags["docker-no-autopush"]);
			if (buildDocker && autopush) core.publishDockerImages({
				ids: flags["package-id"],
				pushTo: flags["docker-push-to"],
				strictPlatformMatching: require_envs.isCI()
			});
		} catch (e) {
			logger.debug(e);
			if (e instanceof Error) logger.debug(e.stack);
			throw e;
		}
	}
};

//#endregion
exports.default = BuildAll;
//# sourceMappingURL=all.cjs.map