import { isCI } from "../../core/envs.js";
import { createLogger } from "../../core/util.js";
import { ArchiveFlag, BuildFlags, CondaFlags, ContentRootFlag, DirHashFlag, DockerFlags, EntrypointNameFlag, ForceFlag, GlobalFlags, PackageIDFlag, PlatformFlags, VersionFlag, modeFromFlag, shouldDoAction } from "../../core/cmd-opts.js";
import { Core } from "../../core/core.js";
import { Command } from "@oclif/core";

//#region src/commands/build/all.ts
var BuildAll = class BuildAll extends Command {
	static description = "Build all targets (entrypoint descriptors, binary pacakges and so on)";
	static examples = ["<%= config.bin %> <%= command.id %>"];
	static flags = {
		...GlobalFlags,
		...ForceFlag,
		...BuildFlags,
		...PlatformFlags,
		...VersionFlag,
		...DockerFlags,
		...CondaFlags,
		...EntrypointNameFlag,
		...PackageIDFlag,
		...DirHashFlag,
		...ArchiveFlag,
		...ContentRootFlag
	};
	async run() {
		const { flags } = await this.parse(BuildAll);
		const logger = createLogger(flags["log-level"]);
		try {
			const core = new Core(logger, { packageRoot: flags["package-root"] });
			core.buildMode = modeFromFlag(flags.dev);
			core.pkgInfo.version = flags.version;
			core.targetPlatform = flags.platform;
			core.allPlatforms = flags["all-platforms"];
			core.fullDirHash = flags["full-dir-hash"];
			const buildDocker = shouldDoAction(isCI(), flags["docker-build"], flags["docker-no-build"]);
			if (buildDocker) core.buildDockerImages({
				ids: flags["package-id"],
				registry: flags["docker-registry"],
				strictPlatformMatching: isCI()
			});
			await core.buildSoftwareArchives({
				ids: flags["package-id"],
				forceBuild: flags.force,
				archivePath: flags.archive,
				contentRoot: flags["content-root"],
				skipIfEmpty: flags["package-id"] ? false : true,
				condaBuild: shouldDoAction(true, flags["conda-build"], flags["conda-no-build"])
			});
			core.buildSwJsonFiles({ packageIds: flags["package-id"] ? flags["package-id"] : void 0 });
			const autopush = shouldDoAction(isCI() && !core.pkgInfo.isPrivate, flags["docker-autopush"], flags["docker-no-autopush"]);
			if (buildDocker && autopush) core.publishDockerImages({
				ids: flags["package-id"],
				pushTo: flags["docker-push-to"],
				strictPlatformMatching: isCI()
			});
		} catch (e) {
			logger.debug(e);
			if (e instanceof Error) logger.debug(e.stack);
			throw e;
		}
	}
};

//#endregion
export { BuildAll as default };
//# sourceMappingURL=all.js.map