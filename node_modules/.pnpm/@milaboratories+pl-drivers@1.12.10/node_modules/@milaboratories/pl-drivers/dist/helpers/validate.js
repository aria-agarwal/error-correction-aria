import path from "node:path";
//#region src/helpers/validate.ts
function validateAbsolute(p) {
	if (!path.isAbsolute(p)) throw new Error(`Path ${p} is not absolute.`);
	return p;
}
//#endregion
export { validateAbsolute };

//# sourceMappingURL=validate.js.map