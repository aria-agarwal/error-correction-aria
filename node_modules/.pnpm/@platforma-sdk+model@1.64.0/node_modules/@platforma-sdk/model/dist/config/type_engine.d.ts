import { Cfg } from "./model.js";
import { ActGetImmediate } from "./actions_kinds.js";

//#region src/config/type_engine.d.ts
type GenericFunction = (x: any) => unknown;
/** This is a type constructor deriving a returned type of config given the
 * context type. Something like ConfAction<Cfg> -> ReturnedType.  */
interface ConfAction {
  readonly ctx: unknown;
  new: GenericFunction;
  isSync: boolean;
}
/** This type basically sets parameter to a ConfAction kind. */
type ActionResult<A extends ConfAction, Ctx> = ReturnType<(A & {
  readonly ctx: Ctx;
})["new"]>;
/** Field key to attach ConfAction information to a config type. */
declare const __config_action__: unique symbol;
/** Creates branded Cfg type */
type TypedConfig<Action extends ConfAction = ConfAction> = Cfg & {
  [__config_action__]: Action;
};
type PrimitiveOrConfig = TypedConfig | string | number | boolean | null;
/** Converts primitive types to immediate config */
type PrimitiveToCfg<T extends PrimitiveOrConfig> = T extends string | number | boolean | null ? TypedConfig<ActGetImmediate<T>> : T;
/** Extracts action */
type ExtractAction<Cfg extends TypedConfig> = Cfg[typeof __config_action__];
type POCExtractAction<T extends PrimitiveOrConfig> = ExtractAction<PrimitiveToCfg<T>>;
type InferVarTypeSafe<Ctx, V> = V extends string ? Ctx extends { [key in V]: infer T } ? T : undefined : unknown;
type ConfigResult<Cfg extends TypedConfig, Ctx> = ActionResult<ExtractAction<Cfg>, Ctx>;
declare const plResourceEntry: unique symbol;
/** Marks that a certain variable in context is a resource entry */
type PlResourceEntry = typeof plResourceEntry;
type OptionalPlResourceEntry = PlResourceEntry | undefined;
//#endregion
export { ActionResult, ConfAction, ConfigResult, ExtractAction, InferVarTypeSafe, OptionalPlResourceEntry, POCExtractAction, PlResourceEntry, PrimitiveOrConfig, PrimitiveToCfg, TypedConfig };
//# sourceMappingURL=type_engine.d.ts.map