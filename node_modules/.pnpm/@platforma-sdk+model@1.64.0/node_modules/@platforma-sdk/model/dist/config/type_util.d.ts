import { ExtractAction, TypedConfig } from "./type_engine.js";

//#region src/config/type_util.d.ts
type SyncConfAction = {
  isSync: true;
};
type IsSyncConf<Cfg extends TypedConfig> = ExtractAction<Cfg> extends SyncConfAction ? true : false;
type CheckedSyncConf<Cfg extends TypedConfig> = Checked<Cfg, IsSyncConf<Cfg>>;
type And<A extends boolean, B extends boolean> = A extends true ? B extends true ? true : false : false;
type IsA<T, E> = T extends E ? true : false;
type Checked<Type, Condition> = Condition extends true ? Type : "error";
//#endregion
export { And, Checked, CheckedSyncConf, IsA, SyncConfAction };
//# sourceMappingURL=type_util.d.ts.map