import { SpawnOptions } from "node:child_process";

//#region src/exec.d.ts
interface SpawnAsyncResult {
  stdout: string;
  stderr: string;
}
/** An async version of `spawn` that returns the stdout and stderr of the process. */
declare function spawnAsync(cmd: string, args: string[], opts: SpawnOptions): Promise<SpawnAsyncResult>;
//#endregion
export { SpawnAsyncResult, spawnAsync };
//# sourceMappingURL=exec.d.ts.map