import { MiLogger } from "./log.js";

//#region src/files.d.ts
declare function fileExists(path: string): Promise<boolean>;
declare function ensureDirExists(fileOrDir: string): Promise<void>;
/** Create an new directory, or empty an existing one */
declare function emptyDir(dirPath: string): Promise<void[]>;
/** Atomically creates a file or a directory, see:
 * https://yakking.branchable.com/posts/atomic-file-creation-tmpfile/
 * fillFileFn should use a syscall that will fail if the temporal file already exists, see (4) */
declare function createPathAtomically(logger: MiLogger, fPath: string, fillFileFn: (fPath: string) => Promise<void>): Promise<void>;
//#endregion
export { createPathAtomically, emptyDir, ensureDirExists, fileExists };
//# sourceMappingURL=files.d.ts.map