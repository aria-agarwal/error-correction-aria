import { EventEmitter } from "node:events";
import { ConsoleLoggerAdapter, MiLogger, MiLoggerLevel, MiLoggerLevel as MiLoggerLevel$1, MiLoggerLogEvent } from "@milaboratories/helpers";

//#region src/log.d.ts
type BlockEventDispatcherEvent = {
  type: "log";
  blockId: string;
  level: MiLoggerLevel;
  message: string;
};
declare class BlockEventDispatcher extends EventEmitter<{
  [key: string]: [BlockEventDispatcherEvent];
}> {
  constructor();
  private eventKey;
  logInfo(blockId: string, message: string): void;
  logWarn(blockId: string, message: string): void;
  logError(blockId: string, message: string): void;
  onBlockEvent(blockId: string, callback: (event: BlockEventDispatcherEvent) => void): () => void;
  removeAllBlockListeners(blockId: string): void;
}
//#endregion
export { BlockEventDispatcher, BlockEventDispatcherEvent, ConsoleLoggerAdapter, type MiLogger, type MiLoggerLevel$1 as MiLoggerLevel, type MiLoggerLogEvent };
//# sourceMappingURL=log.d.ts.map