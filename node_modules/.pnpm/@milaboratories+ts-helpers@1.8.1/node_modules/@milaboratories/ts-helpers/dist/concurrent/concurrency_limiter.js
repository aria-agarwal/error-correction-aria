import Denque from "denque";

//#region src/concurrent/concurrency_limiter.ts
/** Unbound async queue */
var ConcurrencyLimitingExecutor = class {
	lockReleases = new Denque();
	constructor(concurrencyLimit) {
		this.concurrencyLimit = concurrencyLimit;
	}
	async awaitSlot() {
		await new Promise((resolve) => this.lockReleases.push(resolve));
	}
	releaseSlot() {
		const release = this.lockReleases.shift();
		if (release !== void 0) release();
	}
	runningTasks = 0;
	async run(task) {
		while (this.runningTasks === this.concurrencyLimit) await this.awaitSlot();
		if (this.runningTasks >= this.concurrencyLimit) throw new Error(`runningTasks >= limit; ${this.runningTasks} >= ${this.concurrencyLimit} (queue: ${this.lockReleases.length})`);
		this.runningTasks++;
		try {
			return await task();
		} finally {
			this.runningTasks--;
			this.releaseSlot();
		}
	}
};

//#endregion
export { ConcurrencyLimitingExecutor };
//# sourceMappingURL=concurrency_limiter.js.map