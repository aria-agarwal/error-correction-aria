//#region src/concurrent/concurrency_limiter.d.ts
/** Unbound async queue */
declare class ConcurrencyLimitingExecutor {
  private readonly concurrencyLimit;
  private readonly lockReleases;
  constructor(concurrencyLimit: number);
  private awaitSlot;
  private releaseSlot;
  private runningTasks;
  run<T>(task: () => Promise<T>): Promise<T>;
}
//#endregion
export { ConcurrencyLimitingExecutor };
//# sourceMappingURL=concurrency_limiter.d.ts.map