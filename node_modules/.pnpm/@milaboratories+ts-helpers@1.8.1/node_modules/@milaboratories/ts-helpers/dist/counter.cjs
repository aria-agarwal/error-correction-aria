
//#region src/counter.ts
/** It's just a set of callerIds that are interested in one value. */
var CallersCounter = class {
	callers = /* @__PURE__ */ new Set();
	isZero() {
		return this.callers.size == 0;
	}
	inc(callerId) {
		const created = this.callers.size == 0;
		this.callers.add(callerId);
		return created;
	}
	dec(callerId) {
		this.callers.delete(callerId);
		return this.callers.size == 0;
	}
};

//#endregion
exports.CallersCounter = CallersCounter;
//# sourceMappingURL=counter.cjs.map