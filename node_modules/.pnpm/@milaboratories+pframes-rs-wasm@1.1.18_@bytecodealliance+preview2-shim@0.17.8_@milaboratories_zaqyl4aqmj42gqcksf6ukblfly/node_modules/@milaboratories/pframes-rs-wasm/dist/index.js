import { expand, collapse, find } from './axes.js';
import { findColumn } from './table.js';
import { PFrame } from './p-frame.js';

const { createPFrame, expandAxes, collapseAxes, findAxis, findTableColumn, } = {
    createPFrame: (spec) => new PFrame(spec),
    expandAxes: expand,
    collapseAxes: collapse,
    findAxis: find,
    findTableColumn: findColumn,
};

export { collapseAxes, createPFrame, expandAxes, findAxis, findTableColumn };
//# sourceMappingURL=index.js.map
