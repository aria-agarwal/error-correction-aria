import { createGetIncrementalId } from './uniqId.js';
import { isPrimitive } from './utils.js';

function hashString(str, seed = 0) {
    let h1 = 0xdeadbeef ^ seed, h2 = 0x41c6ce57 ^ seed;
    for (let i = 0, ch; i < str.length; i++) {
        ch = str.charCodeAt(i);
        h1 = Math.imul(h1 ^ ch, 2654435761);
        h2 = Math.imul(h2 ^ ch, 1597334677);
    }
    h1 = Math.imul(h1 ^ (h1 >>> 16), 2246822507) ^ Math.imul(h2 ^ (h2 >>> 13), 3266489909);
    h2 = Math.imul(h2 ^ (h2 >>> 16), 2246822507) ^ Math.imul(h1 ^ (h1 >>> 13), 3266489909);
    return 4294967296 * (2097151 & h2) + (h1 >>> 0);
}
function shallowHash(...values) {
    let str = "";
    for (let i = 0; i < values.length; i += 1) {
        const value = values[i];
        if (isPrimitive(value)) {
            str += "/" + String(value);
        }
        else {
            str += "/" + getIdForPointer(value);
        }
    }
    return hashString(str);
}
const mapPointerToMap = new WeakMap();
const getIncrementalId = createGetIncrementalId();
function getIdForPointer(obj) {
    if (!mapPointerToMap.has(obj)) {
        mapPointerToMap.set(obj, getIncrementalId().toString());
    }
    return mapPointerToMap.get(obj);
}

export { hashString, shallowHash };
//# sourceMappingURL=hash.js.map
