import { Computable } from "./computable.js";
import { ComputableHooks } from "./computable_hooks.js";

//#region src/computable/hooks_util.d.ts
type StartStopComputableHooksOps = {
  /** How long to wait after last computable request to send stopUpdating
   * request to the data source. */
  stopDebounce: number;
};
/**
 * Allows to simplify linkage of computable state request events (i.e. signs
 * that somebody is interested in its value) to the processes keeping the
 * underlying state fresh, like periodically polling pl.
 * */
declare class PollingComputableHooks implements ComputableHooks {
  private readonly startUpdating;
  private readonly stopUpdating;
  private readonly scheduleOnNextFreshState?;
  private readonly stopDebounce;
  private sourceActivated;
  constructor(startUpdating: () => void, stopUpdating: () => void, ops: StartStopComputableHooksOps, scheduleOnNextFreshState?: ((resolve: () => void, reject: (error: unknown) => void) => void) | undefined);
  private stopCountdown;
  private scheduleStopIfNeeded;
  private startIfNeeded;
  onChangedRequest(): void;
  onGetValue(_instance: Computable<unknown>): void;
  private readonly awaitingRefresh;
  refreshState(): Promise<void>;
  private readonly listening;
  onListenStart(instance: Computable<unknown>): void;
  onListenStop(instance: Computable<unknown>): void;
}
//#endregion
export { PollingComputableHooks, StartStopComputableHooksOps };
//# sourceMappingURL=hooks_util.d.ts.map