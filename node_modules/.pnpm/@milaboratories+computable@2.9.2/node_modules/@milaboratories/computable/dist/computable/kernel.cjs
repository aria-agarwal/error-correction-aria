const require_runtime = require('../_virtual/_rolldown/runtime.cjs');
let _milaboratories_ts_helpers = require("@milaboratories/ts-helpers");

//#region src/computable/kernel.ts
function tryExtractComputableKernel(v) {
	if (typeof v === "object" && v !== null) {
		if ("___kernel___" in v) return v;
		if ("___wrapped_kernel___" in v) return v["___wrapped_kernel___"];
	}
}
function containComputables(v) {
	const type = typeof v;
	switch (type) {
		case "function":
		case "bigint":
		case "number":
		case "string":
		case "boolean":
		case "symbol":
		case "undefined": return false;
		case "object":
			if (v === null) return false;
			if (tryExtractComputableKernel(v) !== void 0) return true;
			else if (Array.isArray(v)) {
				for (const nested of v) if (containComputables(nested)) return true;
			} else if (v instanceof DataView || v instanceof Date || v instanceof Int8Array || v instanceof Uint8Array || v instanceof Uint8ClampedArray || v instanceof Int16Array || v instanceof Uint16Array || v instanceof Int32Array || v instanceof Uint32Array || v instanceof Float32Array || v instanceof Float64Array || v instanceof BigInt64Array || v instanceof BigUint64Array) return false;
			else for (const [, nested] of Object.entries(v)) if (nested !== v) {
				if (containComputables(nested)) return true;
			}
			return false;
		default: (0, _milaboratories_ts_helpers.assertNever)(type);
	}
}

//#endregion
exports.containComputables = containComputables;
exports.tryExtractComputableKernel = tryExtractComputableKernel;
//# sourceMappingURL=kernel.cjs.map