const require_runtime = require('../_virtual/_rolldown/runtime.cjs');
let upath = require("upath");
upath = require_runtime.__toESM(upath);
let node_fs_promises = require("node:fs/promises");
node_fs_promises = require_runtime.__toESM(node_fs_promises);

//#region src/common/storages.ts
function newRemoteConfigStoragesFS(workdir, localHTTPPort) {
	const workPath = upath.default.join(workdir, "storages", "work");
	const mainPath = upath.default.join(workdir, "storages", "main");
	if (!localHTTPPort) throw new Error("httpPort is required for remote FS storage config generation");
	const main = {
		main: {
			mode: "primary",
			downloadable: true
		},
		storage: {
			id: "main",
			type: "FS",
			indexCachePeriod: "0s",
			rootPath: mainPath,
			externalURL: `http://localhost:${localHTTPPort}`,
			allowRemoteAccess: true
		}
	};
	return {
		runner: workPath,
		dirsToCreate: [workPath, mainPath],
		storages: [
			{
				localPath: "",
				main: {
					mode: "passive",
					downloadable: true
				},
				storage: {
					id: "remoteRoot",
					type: "FS",
					indexCachePeriod: "1m",
					rootPath: ""
				}
			},
			{
				main: {
					mode: "active",
					downloadable: false
				},
				storage: {
					id: "work",
					type: "FS",
					indexCachePeriod: "1m",
					rootPath: workPath
				}
			},
			main
		],
		mainStoragePath: mainPath
	};
}
function newRemoteConfigStoragesMinio(workdir, minioOpts) {
	const workPath = upath.default.join(workdir, "storages", "work");
	const mainPath = upath.default.join(workdir, "storages", "main");
	const main = {
		main: {
			mode: "primary",
			downloadable: true
		},
		storage: {
			id: "main",
			type: "S3",
			indexCachePeriod: "0s",
			endpoint: minioOpts.endpoint,
			region: "",
			presignEndpoint: minioOpts.presignEndpoint,
			bucketName: minioOpts.bucketName,
			createBucket: true,
			forcePathStyle: true,
			key: `static:${minioOpts.key}`,
			secret: `static:${minioOpts.secret}`,
			keyPrefix: "",
			accessPrefixes: [""],
			uploadKeyPrefix: ""
		}
	};
	return {
		runner: workPath,
		dirsToCreate: [workPath, mainPath],
		storages: [
			{
				localPath: "",
				main: {
					mode: "passive",
					downloadable: true
				},
				storage: {
					id: "remoteRoot",
					type: "FS",
					indexCachePeriod: "1m",
					rootPath: "",
					allowRemoteAccess: false,
					externalURL: ""
				}
			},
			{
				main: {
					mode: "active",
					downloadable: false
				},
				storage: {
					id: "work",
					type: "FS",
					indexCachePeriod: "1m",
					rootPath: workPath,
					allowRemoteAccess: false,
					externalURL: ""
				}
			},
			main
		],
		mainStoragePath: mainPath
	};
}
async function createDefaultLocalStorages(workdir, externalURL) {
	const storages = newDefaultLocalStorages(workdir, externalURL);
	for (const d of storages.dirsToCreate) await node_fs_promises.default.mkdir(d, { recursive: true });
	return storages;
}
function newDefaultLocalStorages(workdir, externalURL) {
	const workPath = upath.default.join(workdir, "storages", "work");
	const mainPath = upath.default.join(workdir, "storages", "main");
	return {
		runner: workPath,
		dirsToCreate: [workPath, mainPath],
		storages: [
			{
				localPath: "",
				main: {
					mode: "passive",
					downloadable: true
				},
				storage: {
					id: "root",
					type: "FS",
					indexCachePeriod: "1m",
					rootPath: "",
					allowRemoteAccess: false,
					externalURL: ""
				}
			},
			{
				main: {
					mode: "active",
					downloadable: false
				},
				storage: {
					id: "work",
					type: "FS",
					indexCachePeriod: "1m",
					rootPath: workPath,
					allowRemoteAccess: false,
					externalURL: ""
				}
			},
			{
				localPath: mainPath,
				main: {
					mode: "primary",
					downloadable: true
				},
				storage: {
					id: "main",
					type: "FS",
					indexCachePeriod: "0m",
					rootPath: mainPath,
					allowRemoteAccess: false,
					externalURL
				}
			}
		],
		mainStoragePath: mainPath
	};
}

//#endregion
exports.createDefaultLocalStorages = createDefaultLocalStorages;
exports.newRemoteConfigStoragesFS = newRemoteConfigStoragesFS;
exports.newRemoteConfigStoragesMinio = newRemoteConfigStoragesMinio;
//# sourceMappingURL=storages.cjs.map