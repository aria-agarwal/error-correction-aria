//#region src/common/ports.d.ts
/** Gets either manually-selected, random or free ports, converts them to localhost or other endpoints.
 * Optionally returns minio ports as well. */
/** Gets ports according to options, and concatenated them with the host. */
declare function getLocalhostEndpoints(opts: PlConfigPorts): Promise<Endpoints>;
declare function getPorts(opts: PlConfigPorts): Promise<Ports>;
type PlConfigPortsCustom = {
  readonly type: "custom";
  readonly ports: Ports;
};
type PlConfigPortsCustomWithMinio = {
  readonly type: "customWithMinio";
  readonly ports: PortsWithMinio;
};
type PlConfigPortsRandom = {
  readonly type: "random";
  readonly from: number;
  readonly to: number;
};
type PlConfigPortsPickFree = {
  readonly type: "pickFree";
};
type Ports = {
  /** Grpc, monitoring and debug ports of Pl Backend. */grpc: number;
  http: number;
  monitoring: number;
  debug: number;
  minio?: number;
  minioConsole?: number;
  httpLocal?: number;
  grpcLocal?: number;
  minioLocal?: number;
};
type Endpoints = {
  grpc: string;
  http: string;
  monitoring: string;
  debug: string;
  minio?: string;
  minioConsole?: string;
  httpLocal?: string;
  grpcLocal?: string;
  minioLocal?: string;
};
type PortsWithMinio = {
  grpc: number;
  http: number;
  monitoring: number;
  debug: number;
  minio: number;
  minioConsole: number;
  grpcLocal: number;
  minioLocal: number;
  httpLocal: number;
};
type PlConfigPorts = PlConfigPortsCustom | PlConfigPortsCustomWithMinio | PlConfigPortsRandom | PlConfigPortsPickFree;
declare function getFreePort(): Promise<number>;
/** Turns ports to endpoints by adding host */
declare function withHost(host: string, localHost: string, ports: Ports): Endpoints;
//#endregion
export { Endpoints, PlConfigPorts, PlConfigPortsCustom, PlConfigPortsCustomWithMinio, PlConfigPortsPickFree, PlConfigPortsRandom, Ports, PortsWithMinio, getFreePort, getLocalhostEndpoints, getPorts, withHost };
//# sourceMappingURL=ports.d.ts.map