const require_runtime = require('../_virtual/_rolldown/runtime.cjs');
let upath = require("upath");
upath = require_runtime.__toESM(upath);
let node_fs_promises = require("node:fs/promises");
node_fs_promises = require_runtime.__toESM(node_fs_promises);

//#region src/common/packageloader.ts
/** Gets a default software loader path and creates it locally. */
async function createDefaultLocalPackageSettings(dir, useGlobalAccess) {
	const conf = packageLoaderConfig(dir, useGlobalAccess);
	await node_fs_promises.default.mkdir(conf.packagesRoot, { recursive: true });
	return conf;
}
function packageLoaderConfig(dir, useGlobalAccess) {
	const conf = { packagesRoot: upath.default.join(dir, "packages") };
	if (useGlobalAccess) conf.registries = [{
		name: "platforma-open",
		endpoints: [{
			type: "url",
			url: "https://bin-ga.pl-open.science"
		}]
	}];
	return conf;
}

//#endregion
exports.createDefaultLocalPackageSettings = createDefaultLocalPackageSettings;
exports.packageLoaderConfig = packageLoaderConfig;
//# sourceMappingURL=packageloader.cjs.map