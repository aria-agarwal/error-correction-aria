import { PlControllerDataMainStoragesSettings, PlControllerDataStoragesSettings } from "./types.js";

//#region src/common/storages.d.ts
/** Settings that are needed for config generation:
 * runner's working dir (usually this is "work" storage),
 * dirs that needs to be created (Pl Backend couldn't do it itself at the moment),
 * storages configs and a path to the main storage (minio could use it). */
interface StoragesSettings {
  runner: string;
  dirsToCreate: string[];
  storages: StorageSettings[];
  mainStoragePath: string;
}
interface StorageSettings {
  storage: PlControllerDataStoragesSettings;
  main: PlControllerDataMainStoragesSettings;
  /** Via this path storage is available for the local observer (like middle-layer in desktop app) */
  localPath?: string;
}
interface RemoteMinioSettings {
  endpoint: string;
  presignEndpoint: string;
  key: string;
  secret: string;
  bucketName: string;
}
declare function newRemoteConfigStoragesFS(workdir: string, localHTTPPort?: number): StoragesSettings;
declare function newRemoteConfigStoragesMinio(workdir: string, minioOpts: RemoteMinioSettings): StoragesSettings;
declare function createDefaultLocalStorages(workdir: string, externalURL: string): Promise<StoragesSettings>;
//#endregion
export { RemoteMinioSettings, StorageSettings, StoragesSettings, createDefaultLocalStorages, newRemoteConfigStoragesFS, newRemoteConfigStoragesMinio };
//# sourceMappingURL=storages.d.ts.map