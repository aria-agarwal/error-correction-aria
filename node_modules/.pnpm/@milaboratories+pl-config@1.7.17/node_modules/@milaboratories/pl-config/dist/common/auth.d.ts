import { PlAuthDriver } from "./types.js";

//#region src/common/auth.d.ts
/** A line in a users.htpasswd config */
interface HtpasswdLine {
  user: string;
  password: string;
}
/** Config is just an array of lines. */
type HtpasswdConfig = HtpasswdLine[];
/** Where a default config should be stored and a stringified content of the config. */
type Htpasswd = {
  filePath: string;
  content: string;
};
/** Writes a config to the local storage. */
declare function createLocalHtpasswdFile(dir: string, config: HtpasswdConfig): Promise<string>;
declare function newHtpasswdFile(dir: string, config: HtpasswdConfig): Htpasswd;
declare function stringifyHtpasswdConfig(lines: HtpasswdConfig): string;
declare function getDefaultAuthMethods(htpasswdAuth: string, jwtKey: string): PlAuthDriver[];
//#endregion
export { Htpasswd, HtpasswdConfig, HtpasswdLine, createLocalHtpasswdFile, getDefaultAuthMethods, newHtpasswdFile, stringifyHtpasswdConfig };
//# sourceMappingURL=auth.d.ts.map