import { PlLicenseSettings } from "./types.js";

//#region src/common/license.d.ts
/** How to get a license. */
type PlLicenseMode = PlLicenseEnv | PlLicensePlain;
type PlLicenseEnv = {
  readonly type: "env";
};
type PlLicensePlain = {
  readonly type: "plain";
  readonly value: string;
};
/** A normalized license value. */
type License = LicenseValue | LicenseFile;
type LicenseValue = {
  readonly type: "value";
  readonly value: string;
};
type LicenseFile = {
  readonly type: "file";
  readonly file: string;
};
declare function getLicense(opts: PlLicenseMode): Promise<License>;
/** Gets a license as a value even if it's stored in a file (it reads the file). */
declare function getLicenseValue(opts: PlLicenseMode): Promise<LicenseValue>;
/** Gets MI_LICENSE, PL_LICENSE envs or reads a file stored in a homedir or is pointed by envs. */
declare function getLicenseFromEnv(): Promise<License>;
/** Compiles a license secret for MiXCR software. */
declare function licenseEnvsForMixcr(license: License): Record<string, string>;
/** Compiles a license for Platforma Backend config. */
declare function licenseForConfig(license: License): PlLicenseSettings;
//#endregion
export { License, LicenseFile, LicenseValue, PlLicenseEnv, PlLicenseMode, PlLicensePlain, getLicense, getLicenseFromEnv, getLicenseValue, licenseEnvsForMixcr, licenseForConfig };
//# sourceMappingURL=license.d.ts.map