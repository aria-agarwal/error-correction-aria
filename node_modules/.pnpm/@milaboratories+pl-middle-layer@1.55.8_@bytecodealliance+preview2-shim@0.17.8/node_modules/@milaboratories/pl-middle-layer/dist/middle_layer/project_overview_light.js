import { ProjectStructureKey } from "../model/project_model.js";
import { allBlocks } from "../model/project_model_util.js";
import { notEmpty } from "@milaboratories/ts-helpers";
import { Computable } from "@milaboratories/computable";
//#region src/middle_layer/project_overview_light.ts
/** Returns derived general project state form the project resource */
function projectOverviewLight(prjEntry) {
	return Computable.make((ctx) => {
		return { listOfBlocks: [...allBlocks(notEmpty(ctx.accessor(prjEntry).node().getKeyValueAsJson(ProjectStructureKey)))].map((b) => b.id) };
	});
}
//#endregion
export { projectOverviewLight };

//# sourceMappingURL=project_overview_light.js.map