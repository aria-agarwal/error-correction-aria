import { mapRecord } from "./util.js";
import { computableFromCfgUnsafe } from "./executor.js";
import { assertNever } from "@milaboratories/ts-helpers";
import { Computable } from "@milaboratories/computable";
//#region src/cfg_render/renderer.ts
function res(result) {
	return {
		type: "ReturnResult",
		result
	};
}
function resOp(result) {
	return () => res(result);
}
const SRMakeObject = (args) => {
	const result = {};
	for (const [k, v] of Object.entries(args)) result[k] = v;
	return resOp(result);
};
const SRFlatten = (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	const result = [];
	for (const nested of source) if (nested instanceof Array) result.push(...nested);
	else result.push(nested);
	return resOp(result);
};
const SRGetResourceField = (args) => {
	const source = args.source;
	const field = args.field;
	if (source === void 0 || field === void 0) return resOp(void 0);
	return ({ cCtx }) => {
		return res(cCtx.accessor(source).node().traverse(field)?.persist());
	};
};
function mapArrayToRecord(elements, cb) {
	const result = {};
	const length = elements.length;
	for (let i = 0; i < length; i++) result[String(i)] = cb(elements[i]);
	return result;
}
function SRMapArrayValues1(ctx, ops) {
	return (args) => {
		const source = args.source;
		if (source === void 0) return resOp(void 0);
		return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRCollectArrayFromArgs(source.length),
			args: mapArrayToRecord(source, (e) => renderCfg({
				...ctx,
				[ops.itVar]: e
			}, ops.mapping))
		});
	};
}
function SRCollectArrayFromArgs(length) {
	return (args) => {
		const result = [];
		for (let i = 0; i < length; i++) result.push(args[String(i)]);
		return resOp(result);
	};
}
function SRMapRecordValues1(ctx, ops) {
	return (args) => {
		const source = args.source;
		if (source === void 0) return resOp(void 0);
		const nextArgs = {};
		for (const [k, v] of Object.entries(source)) nextArgs[k] = renderCfg({
			...ctx,
			[ops.itVar]: v
		}, ops.mapping);
		return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRMapRecordValues2,
			args: nextArgs
		});
	};
}
const SRMapRecordValues2 = (args) => {
	return resOp(args);
};
const SRIsEmpty = (args) => {
	const arg = args.arg;
	if (arg === void 0) return resOp(void 0);
	return resOp(arg.length === 0);
};
const SRNot = (args) => {
	const operand = args.operand;
	if (operand === void 0) return resOp(void 0);
	return resOp(!operand);
};
const SRAnd = (args) => {
	const operand1 = args.operand1;
	const operand2 = args.operand2;
	if (operand1 === void 0 || operand2 === void 0) return resOp(void 0);
	return resOp(operand1 && operand2);
};
const SROr = (args) => {
	const operand1 = args.operand1;
	const operand2 = args.operand2;
	if (operand1 === void 0 || operand2 === void 0) return resOp(void 0);
	return resOp(operand1 || operand2);
};
const SRResourceValueAsJson = (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ cCtx }) => res(cCtx.accessor(source).node()?.getDataAsJson());
};
const SRGetJsonField = (args) => {
	const source = args.source;
	const field = args.field;
	if (source === void 0 || field === void 0) return resOp(void 0);
	return resOp(source[field]);
};
function SRMapResourceFields1(ctx, ops) {
	return (args) => {
		const source = args.source;
		if (source === void 0) return resOp(void 0);
		return ({ cCtx }) => {
			const node = cCtx.accessor(source).node();
			const nextArgs = {};
			for (const fieldName of node.listInputFields()) {
				const res = node.traverse(fieldName);
				if (res === void 0) nextArgs[fieldName] = resOp(void 0);
				else nextArgs[fieldName] = renderCfg({
					...ctx,
					[ops.itVar]: res.persist()
				}, ops.mapping);
			}
			return {
				type: "ScheduleSubroutine",
				subroutine: SRMapResourceFields2,
				args: nextArgs
			};
		};
	};
}
const SRMapResourceFields2 = (args) => {
	return resOp(args);
};
const SRGetBlobContent = (range) => (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: Computable.make((ctx) => {
				return drivers.blobDriver.getDownloadedBlob(ctx.accessor(source).node().resourceInfo);
			}, { postprocessValue: async (value) => {
				if (value === void 0) return void 0;
				return await drivers.blobDriver.getContent(value.handle, range);
			} })
		};
	};
};
const SRGetBlobContentAsString = (range) => (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ cCtx, drivers }) => {
		const resourceInfo = cCtx.accessor(source).node().resourceInfo;
		return {
			type: "ScheduleComputable",
			computable: Computable.make((ctx) => drivers.blobDriver.getDownloadedBlob(resourceInfo, ctx), { postprocessValue: async (value) => {
				if (value === void 0) return void 0;
				return (await drivers.blobDriver.getContent(value.handle, range)).toString();
			} })
		};
	};
};
const SRGetBlobContentAsJson = (range) => (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: Computable.make((c) => drivers.blobDriver.getDownloadedBlob(c.accessor(source).node().resourceInfo, c), { postprocessValue: async (value) => {
				if (value == void 0) return void 0;
				const content = await drivers.blobDriver.getContent(value.handle, range);
				if (content == void 0) return void 0;
				return JSON.parse(Buffer.from(content).toString());
			} })
		};
	};
};
const SRGetDownloadedBlobContent = (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return (ctx) => {
		return {
			type: "ScheduleComputable",
			computable: ctx.drivers.blobDriver.getDownloadedBlob(source)
		};
	};
};
const SRGetOnDemandBlobContent = (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: drivers.blobDriver.getOnDemandBlob(source)
		};
	};
};
const SRExtractArchiveAndGetURL = (format) => (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: drivers.blobToURLDriver.extractArchiveAndGetURL(source, format)
		};
	};
};
const SRGetImportProgress = (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: drivers.uploadDriver.getProgressId(source)
		};
	};
};
const SRGetLastLogs = (lines) => (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: drivers.logDriver.getLastLogs(source, lines)
		};
	};
};
const SRGetProgressLog = (patternToSearch) => (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: drivers.logDriver.getProgressLog(source, patternToSearch)
		};
	};
};
const SRGetProgressLogWithInfo = (patternToSearch) => (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: drivers.logDriver.getProgressLogWithInfo(source, patternToSearch)
		};
	};
};
const SRGetLogHandle = (args) => {
	const source = args.source;
	if (source === void 0) return resOp(void 0);
	return ({ drivers }) => {
		return {
			type: "ScheduleComputable",
			computable: drivers.logDriver.getLogHandle(source)
		};
	};
};
/** Renders configuration into executor's Operation */
function renderCfg(ctx, cfg) {
	switch (cfg.type) {
		case "GetFromCtx": {
			const ctxValue = ctx[cfg.variable];
			if (typeof ctxValue === "function") return (e) => res(ctxValue(e.cCtx));
			else return resOp(ctxValue);
		}
		case "Isolate": return ({ drivers }) => ({
			type: "ScheduleComputable",
			computable: computableFromCfgUnsafe(drivers, ctx, cfg.cfg)
		});
		case "Immediate": return resOp(cfg.value);
		case "GetJsonField": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetJsonField,
			args: {
				source: renderCfg(ctx, cfg.source),
				field: renderCfg(ctx, cfg.field)
			}
		});
		case "MapArrayValues": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRMapArrayValues1(ctx, cfg),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "MapRecordValues": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRMapRecordValues1(ctx, cfg),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "MakeObject": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRMakeObject,
			args: mapRecord(cfg.template, (c) => renderCfg(ctx, c))
		});
		case "MakeArray": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRCollectArrayFromArgs(cfg.template.length),
			args: mapArrayToRecord(cfg.template, (e) => renderCfg(ctx, e))
		});
		case "Flatten": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRFlatten,
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "IsEmpty": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRIsEmpty,
			args: { arg: renderCfg(ctx, cfg.arg) }
		});
		case "Not": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRNot,
			args: { operand: renderCfg(ctx, cfg.operand) }
		});
		case "And": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRAnd,
			args: {
				operand1: renderCfg(ctx, cfg.operand1),
				operand2: renderCfg(ctx, cfg.operand2)
			}
		});
		case "Or": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SROr,
			args: {
				operand1: renderCfg(ctx, cfg.operand1),
				operand2: renderCfg(ctx, cfg.operand2)
			}
		});
		case "MapResourceFields": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRMapResourceFields1(ctx, cfg),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetResourceField": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetResourceField,
			args: {
				source: renderCfg(ctx, cfg.source),
				field: renderCfg(ctx, cfg.field)
			}
		});
		case "GetResourceValueAsJson": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRResourceValueAsJson,
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetBlobContent": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetBlobContent(cfg.range),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetBlobContentAsString": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetBlobContentAsString(cfg.range),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetBlobContentAsJson": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetBlobContentAsJson(cfg.range),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetDownloadedBlobContent": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetDownloadedBlobContent,
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetOnDemandBlobContent": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetOnDemandBlobContent,
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "ExtractArchiveAndGetURL": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRExtractArchiveAndGetURL(cfg.format),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetImportProgress": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetImportProgress,
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetLastLogs": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetLastLogs(cfg.lines),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetProgressLog": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetProgressLog(cfg.patternToSearch),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetProgressLogWithInfo": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetProgressLogWithInfo(cfg.patternToSearch),
			args: { source: renderCfg(ctx, cfg.source) }
		});
		case "GetLogHandle": return () => ({
			type: "ScheduleSubroutine",
			subroutine: SRGetLogHandle,
			args: { source: renderCfg(ctx, cfg.source) }
		});
		default: return assertNever(cfg);
	}
}
//#endregion
export { renderCfg, resOp };

//# sourceMappingURL=renderer.js.map