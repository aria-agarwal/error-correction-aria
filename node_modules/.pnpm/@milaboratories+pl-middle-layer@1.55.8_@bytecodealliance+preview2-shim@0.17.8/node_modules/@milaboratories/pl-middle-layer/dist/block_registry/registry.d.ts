import { V2RegistryProvider } from "./registry-v2-provider.js";
import { BlockPackId, BlockPackListing, RegistryEntry, SingleBlockPackOverview } from "@milaboratories/pl-model-middle-layer";
import { Dispatcher } from "undici";
import { BlockPackDescriptionAbsolute } from "@platforma-sdk/block-tools";

//#region src/block_registry/registry.d.ts
declare function getDevV1PacketMtime(devPath: string): Promise<string>;
declare function getDevV2PacketMtime(description: BlockPackDescriptionAbsolute): Promise<string>;
declare class BlockPackRegistry {
  private readonly v2Provider;
  private readonly registries;
  private readonly http?;
  constructor(v2Provider: V2RegistryProvider, registries: RegistryEntry[], http?: Dispatcher | undefined);
  private getPackagesForRoot;
  listBlockPacks(): Promise<BlockPackListing>;
  getOverview(registryId: string, blockId: BlockPackId, channel: string): Promise<SingleBlockPackOverview>;
}
//#endregion
export { BlockPackRegistry, getDevV1PacketMtime, getDevV2PacketMtime };
//# sourceMappingURL=registry.d.ts.map