import { V2RegistryProvider } from "./registry-v2-provider.js";
import { BlockPackSpec, BlockSettings, UpdateSuggestions } from "@milaboratories/pl-model-middle-layer";
import { Dispatcher } from "undici";
import { MiLogger } from "@milaboratories/ts-helpers";
import { PollComputablePool, PollPoolOps } from "@milaboratories/computable";

//#region src/block_registry/watcher.d.ts
type BlockUpdateWatcherOps = Partial<PollPoolOps> & {
  readonly http?: Dispatcher;
  readonly preferredUpdateChannel?: string;
};
type CheckForUpdateRequest = {
  currentSpec: BlockPackSpec;
  settings: BlockSettings;
};
type CheckForUpdateResponse = {
  suggestions: UpdateSuggestions;
  mainSuggestion?: BlockPackSpec;
};
declare class BlockUpdateWatcher extends PollComputablePool<CheckForUpdateRequest, CheckForUpdateResponse> {
  private readonly registryProvider;
  private readonly http?;
  private readonly preferredUpdateChannel?;
  constructor(registryProvider: V2RegistryProvider, logger: MiLogger, ops?: BlockUpdateWatcherOps);
  protected getKey(req: CheckForUpdateRequest): string;
  protected readValue(req: CheckForUpdateRequest): Promise<CheckForUpdateResponse>;
  protected resultsEqual(res1: CheckForUpdateResponse, res2: CheckForUpdateResponse): boolean;
}
//#endregion
export { BlockUpdateWatcher };
//# sourceMappingURL=watcher.d.ts.map