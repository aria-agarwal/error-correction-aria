const require_runtime = require("../_virtual/_rolldown/runtime.cjs");
let node_path = require("node:path");
node_path = require_runtime.__toESM(node_path);
let node_worker_threads = require("node:worker_threads");
//#region src/worker/WorkerManager.ts
var WorkerManager = class {
	nextMessageId = 0;
	worker;
	pendingRequests = /* @__PURE__ */ new Map();
	constructor() {
		const worker = this.worker = new node_worker_threads.Worker(typeof global !== "undefined" && "__WORKER_PATH__" in global ? global.__WORKER_PATH__ : node_path.default.resolve(__dirname, `worker.${typeof module !== "undefined" ? "cjs" : "js"}`), { workerData: {} });
		worker.on("message", ({ id, data, error }) => {
			const { resolve, reject } = this.pendingRequests.get(id);
			this.pendingRequests.delete(id);
			if (error) reject(new Error(error));
			else resolve(data);
		});
		worker.on("error", (msg) => {
			console.error("Worker error", msg);
			this.rejectAllPendingRequests(msg);
		});
	}
	rejectAllPendingRequests(msg) {
		Array.from(this.pendingRequests.values()).map(({ reject }) => {
			reject(msg);
		});
	}
	async process(type, ...args) {
		return new Promise((resolve, reject) => {
			const id = ++this.nextMessageId;
			this.pendingRequests.set(id, {
				resolve,
				reject
			});
			this.worker.postMessage({
				id,
				type,
				args
			});
		});
	}
	async terminate() {
		this.rejectAllPendingRequests(/* @__PURE__ */ new Error("Worker terminated"));
		await this.worker.terminate();
	}
	[Symbol.asyncDispose]() {
		return this.terminate();
	}
};
//#endregion
exports.WorkerManager = WorkerManager;

//# sourceMappingURL=WorkerManager.cjs.map