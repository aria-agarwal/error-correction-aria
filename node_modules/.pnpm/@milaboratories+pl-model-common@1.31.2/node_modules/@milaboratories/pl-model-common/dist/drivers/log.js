//#region src/drivers/log.ts
/** Prefix constants — single source of truth for handle format. */
const LIVE_LOG_PREFIX = "log+live://log/";
const READY_LOG_PREFIX = "log+ready://log/";
/** Type guard to check if a value is any kind of log handle. */
function isAnyLogHandle(value) {
	return typeof value === "string" && (value.startsWith(LIVE_LOG_PREFIX) || value.startsWith(READY_LOG_PREFIX));
}
/** Type guard to check if log is live, and corresponding process is not finished. */
function isLiveLog(handle) {
	return handle !== void 0 && handle.startsWith(LIVE_LOG_PREFIX);
}
//#endregion
export { isAnyLogHandle, isLiveLog };

//# sourceMappingURL=log.js.map