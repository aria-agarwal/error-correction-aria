import { Fetcher as e } from "../lib/util/helpers/dist/utils.js";
import "../lib/util/helpers/dist/index.js";
import { computed as t, effectScope as n, getCurrentScope as r, onScopeDispose as i, shallowRef as a } from "vue";
import { getRawPlatformaInstance as o } from "@platforma-sdk/model";
import { LRUCache as s } from "lru-cache";
//#region src/composition/fileContent.ts
var c = { cacheSize: 15e6 }, l = class {
	_str = void 0;
	_rawJson = void 0;
	_zodSchema = void 0;
	_validatedJson = void 0;
	constructor(e) {
		this.bytes = e;
	}
	get str() {
		return this._str === void 0 && (this._str = new TextDecoder().decode(this.bytes)), this._str;
	}
	get rawJson() {
		return this._rawJson === void 0 && (this._rawJson = JSON.parse(this.str)), this._rawJson;
	}
	validatedJson(e) {
		return this._zodSchema !== e && (this._validatedJson = e.safeParse(this.rawJson), this._zodSchema = e), this._validatedJson?.success ? this._validatedJson.data : void 0;
	}
}, u = /* @__PURE__ */ new WeakMap();
function d(e) {
	u.set(e, /* @__PURE__ */ new Map());
}
var f = new s({
	maxSize: c.cacheSize,
	sizeCalculation: (e) => e.bytes.length
}), p = new e(), m = class m {
	fileDataCache;
	fetcher;
	ns = /* @__PURE__ */ new Map();
	currentKey;
	constructor(t, n) {
		this.currentScope = t;
		let r = {
			...c,
			...n
		};
		this.fileDataCache = r.lruCache ?? new s({
			maxSize: r.cacheSize,
			sizeCalculation: (e) => e.bytes.length
		}), this.fetcher = r.fetcher ?? new e();
	}
	withInvalidate(e, t) {
		let n = this.ns.get(e);
		this.ns.set(e, /* @__PURE__ */ new Set()), this.currentKey = e;
		try {
			let r = t();
			return this.invalidate(e, n), r;
		} finally {
			this.currentKey = void 0;
		}
	}
	stopScope() {
		u.delete(this.currentScope), this.currentScope.stop();
	}
	async doFetch(e) {
		if (!this.fileDataCache.has(e)) {
			let t = await this.fetcher.fetch(e, async () => new l(await o().blobDriver.getContent(e)));
			this.fileDataCache.set(e, t);
		}
		return this.fileDataCache.get(e);
	}
	getSize() {
		let e = this.getRefsMap();
		return e ? e.size : 0;
	}
	getRefsMap() {
		return u.get(this.currentScope);
	}
	invalidate(e, t) {
		if (!t) return;
		let n = this.ns.get(e);
		for (let e of n) t.delete(e);
		let r = this.getRefsMap();
		for (let e of t) r?.delete(e);
	}
	withHandle(e) {
		this.currentKey && this.ns.get(this.currentKey)?.add(e);
	}
	getDataRef(e) {
		let t = this.getRefsMap();
		if (!t) throw Error("ReactiveFileContent must be used within a Vue component or effect scope.");
		this.withHandle(e);
		let n = t.get(e);
		if (n !== void 0) return n;
		let r = a();
		return t.set(e, r), (async () => {
			for (let t = 0; t < 8; t++) try {
				r.value = await this.doFetch(e);
				return;
			} catch (e) {
				console.error(`File download attempt ${t + 1}/8 failed:`, e), t < 7 ? await new Promise((e) => setTimeout(e, 1e3)) : console.error("Failed to download file after 8 attempts");
			}
		})(), r;
	}
	getContentBytes(e) {
		if (e === void 0) return;
		let n = this.getDataRef(e);
		return t(() => n.value?.bytes);
	}
	getContentString(e) {
		if (e === void 0) return;
		let n = this.getDataRef(e);
		return t(() => n.value?.str);
	}
	getContentJson(e, n) {
		if (e === void 0) return;
		let r = this.getDataRef(e);
		return t(() => n === void 0 ? r.value?.rawJson : r.value?.validatedJson(n));
	}
	static initScope(e) {
		let t = r() ?? e;
		return t ||= (console.warn("Current scope not found, using new detached scope..."), n(!0)), d(t), i(() => {
			u.delete(t);
		}), t;
	}
	static use(e, t) {
		return new m(this.initScope(t), { ...e });
	}
	static useGlobal(e, t) {
		return new m(this.initScope(t), {
			...e,
			lruCache: f,
			fetcher: p
		});
	}
};
//#endregion
export { m as ReactiveFileContent };

//# sourceMappingURL=fileContent.js.map