//#region ../../lib/util/helpers/dist/prettyBytes.js
var e = [
	"B",
	"kB",
	"MB",
	"GB",
	"TB",
	"PB",
	"EB",
	"ZB",
	"YB"
], t = [
	"B",
	"kiB",
	"MiB",
	"GiB",
	"TiB",
	"PiB",
	"EiB",
	"ZiB",
	"YiB"
], n = [
	"b",
	"kbit",
	"Mbit",
	"Gbit",
	"Tbit",
	"Pbit",
	"Ebit",
	"Zbit",
	"Ybit"
], r = [
	"b",
	"kibit",
	"Mibit",
	"Gibit",
	"Tibit",
	"Pibit",
	"Eibit",
	"Zibit",
	"Yibit"
], i = (e, t) => {
	let n = String(e);
	return typeof t == "string" || Array.isArray(t) ? n = e.toLocaleString(t) : t === !0 && (n = e.toLocaleString(void 0)), n;
};
function a(a, o) {
	if (a = typeof a == "bigint" ? Number(a) : a, !Number.isFinite(a)) throw TypeError(`Expected a finite number, got ${typeof a}: ${a}`);
	Object.assign({
		bits: !1,
		binary: !1
	}, o);
	let s = o.bits ? o.binary ? r : n : o.binary ? t : e;
	if (o.signed && a === 0) return ` 0 ${s[0]}`;
	let c = a < 0, l = c ? "-" : o.signed ? "+" : "";
	if (c && (a = -a), a < 1) return l + i(a, o.locale) + " " + s[0];
	let u = Math.min(Math.floor(o.binary ? Math.log(a) / Math.log(1024) : Math.log10(a) / 3), s.length - 1);
	a /= (o.binary ? 1024 : 1e3) ** u, a = Number(a.toPrecision(3));
	let d = i(Number(a), o.locale), f = s[u];
	return l + d + " " + f;
}
//#endregion
export { a as prettyBytes };

//# sourceMappingURL=prettyBytes.js.map