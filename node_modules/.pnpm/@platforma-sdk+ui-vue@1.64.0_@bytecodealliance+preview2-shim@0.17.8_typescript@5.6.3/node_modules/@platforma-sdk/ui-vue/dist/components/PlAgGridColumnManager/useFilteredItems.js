import { computed as e, toValue as t } from "vue";
//#region src/components/PlAgGridColumnManager/useFilteredItems.ts
function n(n) {
	let i = e(() => {
		let { items: e, query: i, getStrings: a } = t(n), o = [], s = /* @__PURE__ */ new Map();
		for (let t of e.value) {
			let e = !1;
			for (let n of a(t)) {
				let a = s.get(n);
				a || (a = r(n, i.value), s.set(n, a)), !e && (!i.value || a.some(({ match: e }) => e)) && (o.push(t), e = !0);
			}
		}
		return {
			filteredItems: o,
			segments: s
		};
	});
	return {
		filteredItems: e(() => i.value.filteredItems),
		segments: e(() => i.value.segments)
	};
}
function r(e, t) {
	if (!t) return [{
		value: e,
		match: !1
	}];
	let n = e.toLowerCase(), r = t.toLowerCase(), i = [], a = 0;
	for (;;) {
		let o = n.indexOf(r, a), s = o + t.length;
		if (o < 0) break;
		a !== o && i.push({
			value: e.slice(a, o),
			match: !1
		});
		let c = i.at(-1);
		c?.match ? c.value += e.slice(o, s) : i.push({
			value: e.slice(o, s),
			match: !0
		}), a = s;
	}
	return a < e.length && i.push({
		value: e.slice(a),
		match: !1
	}), i;
}
//#endregion
export { n as useFilteredItems };

//# sourceMappingURL=useFilteredItems.js.map