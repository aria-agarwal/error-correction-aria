import { isJsonEqual as e } from "../../../lib/util/helpers/dist/objects.js";
import "../../../lib/util/helpers/dist/index.js";
import { PlAgDataTableRowNumberColId as t, makeRowNumberColDef as n } from "./row-number.js";
import r from "../../PlAgColumnHeader/PlAgColumnHeader.js";
import "../../PlAgColumnHeader/index.js";
import i from "../../PlAgTextAndButtonCell/PlAgTextAndButtonCell.js";
import "../../PlAgTextAndButtonCell/index.js";
import { PTableHidden as a } from "./common.js";
import { defaultMainMenuItems as o } from "./menu-items.js";
import { getColumnRenderingSpec as s } from "./value-rendering.js";
import { Annotation as c, ValueType as l, canonicalizeJson as u, getAxisId as d, getPTableColumnId as f, isColumnHidden as p, isColumnOptional as m, isLabelColumn as h, isLinkerColumn as g, matchAxisId as _, pTableValue as v, readAnnotation as y, readAnnotationJson as b } from "@platforma-sdk/model";
import { isNil as x, uniq as S } from "es-toolkit";
//#region src/components/PlAgDataTable/sources/table-source-v2.ts
function C(e, t, n, r) {
	let i = [];
	for (let o = 0; o < t[0].data.length; ++o) {
		let s = r.map((e) => v(t[e], o)), c = u(s), l = e.reduce((e, r, i) => (e[r.toString()] = n[i] === -1 ? a : v(t[n[i]], o), e), {
			id: c,
			axesKey: s
		});
		i.push(l);
	}
	return i;
}
async function w({ generation: r, pfDriver: i, model: a, sheets: o, dataRenderedTracker: s, hiddenColIds: c, cellButtonAxisParams: l }) {
	let u = r.value, [d, f] = await Promise.all([i.getSpec(a.fullTableHandle), i.getSpec(a.visibleTableHandle)]);
	if (u !== r.value) throw Error("table state generation changed");
	let p = E(d, f), m = D(o), h = O(d, m), g = A(k(d, m), d), _ = j(g, d, h), v = c ?? M(g, _, d), y = [n(), ...g.map((e, t) => T(e, d[e], d[_[t]], v, l))], b = N(f), { requestIndices: x, fieldResultMapping: S, axesResultIndices: w } = P(_, p, b.map(([e]) => e)), F = -1, I;
	return {
		axesSpec: b.map(([, { spec: e }]) => e),
		columnDefs: y,
		serverSideDatasource: { getRows: async (n) => {
			if (u !== r.value) return n.fail();
			try {
				if (F === -1) {
					let e = await i.getShape(a.visibleTableHandle);
					if (u !== r.value || n.api.isDestroyed()) return n.fail();
					F = e.rows;
				}
				if (F == 0) {
					n.success({
						rowData: [],
						rowCount: F
					}), n.api.setGridOption("loading", !1), n.api.showNoRowsOverlay();
					return;
				}
				if (I && !e(I.request.sortModel, n.request.sortModel)) return n.success({
					rowData: [],
					rowCount: F
				});
				I = n;
				let o = 0, c = [];
				if (F > 0 && n.request.startRow !== void 0 && n.request.endRow !== void 0 && (o = Math.min(F, n.request.endRow) - n.request.startRow, o > 0)) {
					let e = await i.getData(a.visibleTableHandle, x, {
						offset: n.request.startRow,
						length: o
					});
					if (u !== r.value || n.api.isDestroyed()) return n.fail();
					c = C(g, e, S, w);
				}
				n.success({
					rowData: c,
					rowCount: F
				}), n.api.autoSizeColumns(n.api.getAllDisplayedColumns().filter((e) => e.getColId() !== t)), n.api.setGridOption("loading", !1), s.resolve(n.api);
			} catch (e) {
				if (u !== r.value || n.api.isDestroyed()) return n.fail();
				n.api.setGridOption("loading", !0), n.fail(), console.trace(e);
			}
		} }
	};
}
function T(t, n, a, d, f) {
	let p = u({
		source: n,
		labeled: a
	}), m = n.type === "axis" ? n.spec.type : n.spec.valueType, h = s(n), g = {};
	h.fontFamily && (h.fontFamily === "monospace" ? (g.fontFamily = "Spline Sans Mono", g.fontWeight = 300) : g.fontFamily = h.fontFamily);
	let _ = y(a.spec, c.Label)?.trim() ?? y(n.spec, c.Label)?.trim() ?? `Unlabeled ${n.type} ${t}`;
	return {
		colId: p,
		mainMenuItems: o,
		context: n,
		field: `${t}`,
		headerName: _,
		lockPosition: n.type === "axis",
		hide: d !== void 0 && d.includes(p),
		valueFormatter: h.valueFormatter,
		headerComponent: r,
		cellRendererSelector: f?.showCellButtonForAxisId ? (t) => {
			if (n.type !== "axis") return;
			let r = t.colDef?.context?.id;
			if (e(r, f.showCellButtonForAxisId)) return {
				component: i,
				params: {
					invokeRowsOnDoubleClick: f.cellButtonInvokeRowsOnDoubleClick,
					onClick: (e) => {
						f.trigger(e.data?.axesKey);
					}
				}
			};
		} : void 0,
		cellStyle: g,
		headerComponentParams: {
			type: (() => {
				switch (m) {
					case l.Int:
					case l.Long:
					case l.Float:
					case l.Double: return "Number";
					case l.String:
					case l.Bytes: return "Text";
					default: throw Error(`unsupported data type: ${m}`);
				}
			})(),
			tooltip: y(a.spec, c.Description)?.trim()
		},
		cellDataType: (() => {
			switch (m) {
				case l.Int:
				case l.Long:
				case l.Float:
				case l.Double: return "number";
				case l.String:
				case l.Bytes: return "text";
				default: throw Error(`unsupported data type: ${m}`);
			}
		})()
	};
}
function E(e, t) {
	let n = (e) => u(f(e)), r = new Map(t.entries().map(([e, t]) => [n(t), e]));
	return new Map(e.entries().map(([e, t]) => {
		let i = r.get(n(t));
		return [e, x(i) ? -1 : i];
	}));
}
function D(e) {
	let t = e.map((e) => d(e.axis));
	return (e) => t.some((t) => _(t, e));
}
function O(e, t) {
	let n = [];
	for (let [r, i] of e.entries()) {
		if (i.type !== "column" || !h(i.spec)) continue;
		let e = d(i.spec.axesSpec[0]);
		if (!t(e)) {
			if (n.some((t) => _(t.axisId, e))) {
				console.warn(`multiple label columns match axisId: ${JSON.stringify(e)}`);
				continue;
			}
			n.push({
				axisId: e,
				labelColumnIdx: r
			});
		}
	}
	return (e) => n.find((t) => _(t.axisId, e))?.labelColumnIdx ?? -1;
}
function k(e, t) {
	return e.entries().filter(([, e]) => {
		switch (e.type) {
			case "axis": return !t(e.id);
			case "column": return !h(e.spec) && !p(e.spec) && !g(e.spec);
		}
	}).map(([e]) => e).toArray();
}
function A(e, t) {
	let n = (e) => {
		let n = t[e];
		return n.type === "column" ? b(n.spec, c.Table.OrderPriority) ?? 0 : 0;
	};
	return [...e].sort((e, r) => t[e].type === t[r].type ? n(r) - n(e) : t[e].type === "axis" ? -1 : 1);
}
function j(e, t, n) {
	return e.map((e) => {
		let r = t[e], i = r.type === "axis" ? n(r.id) : -1;
		return i === -1 ? e : i;
	});
}
function M(e, t, n) {
	return e.reduce((e, r, i) => {
		let a = n[r];
		if (a.type !== "column" || !m(a.spec)) return e;
		let o = n[t[i]];
		return [...e, u({
			source: a,
			labeled: o
		})];
	}, []);
}
function N(e) {
	return e.entries().filter((e) => e[1].type === "axis").toArray();
}
function P(e, t, n) {
	let r = e.map((e) => {
		let n = t.get(e);
		return n === void 0 || n === -1 ? null : n;
	}), i = r.filter((e) => e !== null), a = [], o = 0;
	for (let e of r) a.push(e === null ? -1 : o++);
	let s = S([...i, ...n]);
	return {
		requestIndices: s,
		fieldResultMapping: a,
		axesResultIndices: n.map((e) => s.indexOf(e))
	};
}
//#endregion
export { w as calculateGridOptions };

//# sourceMappingURL=table-source-v2.js.map